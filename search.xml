<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello!</title>
    <url>/2023/03/06/Hello/</url>
    <content><![CDATA[<h2 id="欢迎来到我的博客！"><a href="#欢迎来到我的博客！" class="headerlink" title="欢迎来到我的博客！"></a>欢迎来到我的博客！</h2>]]></content>
      <categories>
        <category>日常</category>
      </categories>
      <tags>
        <tag>日常</tag>
      </tags>
  </entry>
  <entry>
    <title>算法-kmp算法</title>
    <url>/2023/03/07/%E7%AE%97%E6%B3%95/%E7%AE%97%E6%B3%95-kmp%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>leetcode题目索引:<a href="https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/">28. 找出字符串中第一个匹配项的下标</a></p>
<h2 id="什么是KMP"><a href="#什么是KMP" class="headerlink" title="什么是KMP"></a>什么是KMP</h2><p>KMP是由这三位学者发明的：Knuth，Morris和Pratt，所以取了三位学者名字的首字母。所以叫做KMP。KMP算法主要用在字符串匹配上，主要思想是<strong>当出现字符串不匹配时，可以知道一部分之前已经匹配的文本内容，可以利用这些信息避免从头再去做匹配了</strong>。</p>
<h2 id="什么是前缀表"><a href="#什么是前缀表" class="headerlink" title="什么是前缀表"></a>什么是前缀表</h2><p>以字符串aabaaf为例</p>
<p>所有不包含第一个字符a的字串即为前缀，有a,ab,aba,abaa,abaaf</p>
<p>所有不包含最后一个字符f的字串即为后缀，有a,aa,aab,aaba,aabaa</p>
<p>前缀表就是记录一个字符串其所有字串中最长相同前缀后缀的长度</p>
<p>如aabaa，就有最长相同长度为2</p>
<p>aaba，就是1</p>
<p>那么对于字符串aabaaf而言，其前缀表即为:</p>
<table>
<thead>
<tr>
<th>0</th>
<th>1</th>
<th>2</th>
<th>3</th>
<th>4</th>
<th>5</th>
</tr>
</thead>
<tbody><tr>
<td>a</td>
<td>aa</td>
<td>aab</td>
<td>aaba</td>
<td>aabaa</td>
<td>aabaaf</td>
</tr>
<tr>
<td>0</td>
<td>1</td>
<td>0</td>
<td>1</td>
<td>2</td>
<td>0</td>
</tr>
</tbody></table>
<h2 id="为什么需要前缀表呢"><a href="#为什么需要前缀表呢" class="headerlink" title="为什么需要前缀表呢"></a>为什么需要前缀表呢</h2><p>我们知道KMP算法的核心思想就是当出现不匹配时，重复利用之前匹配过的结果以减少时间复杂度。可以重复利用的部分就是相同的前缀和后缀。</p>
<p>因此，当出现不匹配的情况时，我们只需要把指针移动到已匹配的字符串中最长前缀长度的后面一位处，再接着进行匹配即可。</p>
<p>以aabaaf为例，如果在f处出现不匹配的情况，那么只需要把指针移到aa(最长相同前后缀长度，2)的后面一位处，即可。对指针而言，移动的步数即为5(当前index)-2(需要移动到的index)&#x3D;3。</p>
<p><img src="https://code-thinking.cdn.bcebos.com/gifs/KMP%E7%B2%BE%E8%AE%B21.gif"></p>
<h2 id="如何构造前缀表？"><a href="#如何构造前缀表？" class="headerlink" title="如何构造前缀表？"></a>如何构造前缀表？</h2><p>显然我们需要对一个字符串的所有字串计算其最长相同前后缀长度，再把它填入到前缀表中。</p>
<p>构建next数组的过程就是构建前缀表的过程，next数组既是对前缀表中每一项-1后的结果。我们使用next数组一样可以完成KMP算法的计算过程；</p>
<p>如何构造next数组？我看了不少资料，没有采用最常见的双指针写法，而是选择了一个自己能够理解的例子来进行书写，此处不能保证代码的效率是最高的。</p>
<blockquote>
<p>具体来说，就是从模式字符串的第一位(注意，不包括第0位)开始对自身进行匹配运算。 在任一位置，能匹配的最长长度就是当前位置的next值。</p>
</blockquote>
<p>其实就是遍历一遍target字符串，对于每一位根据ptr在原target中进行比较。若不同，则<u>需查询他前面一个字串的最长相同前后缀长度，然后赋值给ptr，直到ptr处的值与i处的值相同或者ptr到达0</u>；如果相同，则把当前的ptr+1，ptr的大小就是相同前缀的长度+1。</p>
<p>这里说明为什么要进行下划线部分，我们知道ptr的值其实就是最长相同前后缀长度+1（把ptr理解成另一个用来匹配的字符串的index即可）那么当出现不匹配的情况，也就是字串的后缀的最后一个字符与当前不同，我们就需要调整前缀。但后缀一定是包括最后一个字符的，我们去调整i的值没有意义，因为一定会包含一个与当前ptr所指位置不同的字符。因此需要向前调整ptr，也就是调整前缀的最后一个字符。</p>
<p>由于ptr之前的字符串与当前已经匹配的后缀（不包括i）是相同的，因此我们只需要调整前缀的最后一个字符。</p>
<p>如何保证ptr之前的字符串与当前已经匹配的后缀（不包括i）是相同的的呢？这时候就要用到已经记录好的next数组，next数组记录了[0,ptr-1]所有字符串的最长相同前后缀长度，也就是ptr的位置。只需要根据next数组调整ptr的位置，再判断&lt;相同的前缀+ptr处新增的前缀的最后一个字符&gt;与&lt;相同的后缀+i处新增的最后一个字符&gt;是否相同即可</p>
<p>下面给出Java代码实现</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">target</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;abababca&quot;</span><br><span class="hljs-type">int</span>[] next = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[needle.length()];<br>next[<span class="hljs-number">0</span>] = -<span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">ptr</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<span class="hljs-comment">//指示另一个用来匹配的字符串</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;i&lt;needle.length();i++)&#123;<br>   <span class="hljs-keyword">while</span>(needle.charAt(i) != needle.charAt(ptr))&#123;<br>       <span class="hljs-keyword">if</span>(ptr&lt;=<span class="hljs-number">0</span>)<span class="hljs-keyword">break</span>;<br>        ptr = next[ptr-<span class="hljs-number">1</span>]+<span class="hljs-number">1</span>;<br>    &#125;<br>   <span class="hljs-keyword">if</span>(needle.charAt(i) == needle.charAt(ptr))&#123;<br>        ptr++;<br>    &#125;<br>    next[i] = ptr-<span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="完整的代码实现"><a href="#完整的代码实现" class="headerlink" title="完整的代码实现"></a>完整的代码实现</h2><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">strStr</span><span class="hljs-params">(String haystack, String needle)</span> &#123;<br>   <span class="hljs-type">int</span>[] next = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>[needle.length()];<br>   next[<span class="hljs-number">0</span>] = -<span class="hljs-number">1</span>;<br>   <span class="hljs-type">int</span> <span class="hljs-variable">ptr</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<span class="hljs-comment">//指示另一个用来匹配的字符串</span><br>   <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;i&lt;needle.length();i++)&#123;<br>       <span class="hljs-keyword">while</span>(needle.charAt(i) != needle.charAt(ptr))&#123;<br>           <span class="hljs-keyword">if</span>(ptr&lt;=<span class="hljs-number">0</span>) <span class="hljs-keyword">break</span>;<br>           ptr = next[ptr-<span class="hljs-number">1</span>]+<span class="hljs-number">1</span>;<br>       &#125;<br>       <span class="hljs-keyword">if</span>(needle.charAt(i) == needle.charAt(ptr))&#123;<br>           ptr++;<br>       &#125;<br>       next[i] = ptr-<span class="hljs-number">1</span>;<br>   &#125;<br>   ptr = <span class="hljs-number">0</span>;<br>   <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span> ;i &lt; haystack.length();i++)&#123;<br>      <span class="hljs-keyword">if</span>(ptr == needle.length()-<span class="hljs-number">1</span>&amp;&amp;haystack.charAt(i)==needle.charAt(ptr))<br>          <span class="hljs-keyword">return</span> i-needle.length()+<span class="hljs-number">1</span> ;<br>      <span class="hljs-keyword">while</span>(ptr!=<span class="hljs-number">0</span>&amp;&amp;haystack.charAt(i)!=needle.charAt(ptr)) ptr = next[ptr-<span class="hljs-number">1</span>]+<span class="hljs-number">1</span>;<br>      <span class="hljs-keyword">if</span>(haystack.charAt(i)==needle.charAt(ptr))ptr++;<br>   &#125;<br>   <span class="hljs-keyword">return</span> -<span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><a href="https://www.programmercarl.com/0028.%E5%AE%9E%E7%8E%B0strStr.html">https://www.programmercarl.com/0028.%E5%AE%9E%E7%8E%B0strStr.html</a> </p>
<p><a href="https://www.zhihu.com/question/21923021/answer/281346746">https://www.zhihu.com/question/21923021/answer/281346746</a></p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>软件系统设计-原型模式</title>
    <url>/2023/03/07/%E5%A4%A7%E5%AD%A6/%E8%BD%AF%E7%BB%9F/%E8%BD%AF%E4%BB%B6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1-%E5%8E%9F%E5%9E%8B%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="模式动机"><a href="#模式动机" class="headerlink" title="模式动机"></a>模式动机</h2><ul>
<li>使用原型模式来复制一个对象本身，从而克隆出多个与原则对象一模一样的对象。</li>
<li>原型模式通过给出一个原型对象来指明所要创建的对象的类型，然后用复制这个原型对象的办法创建出更多同类型的对象 这就是原型模式的意图所在 。</li>
<li>符合迪米特法则（最小知识原则），屏蔽了细节。</li>
</ul>
<h2 id="模式定义"><a href="#模式定义" class="headerlink" title="模式定义"></a>模式定义</h2><ul>
<li>是一种对象创建型模式， 用原型实例指定创建对象的种类，并且通过复制这些原型创建新的对象。 原型模式允许一个对象再创建另外一个可定制的对象，无须知道任何创建的细节。</li>
</ul>
<h2 id="模式结构"><a href="#模式结构" class="headerlink" title="模式结构"></a>模式结构</h2><p><img src="https://s3.bmp.ovh/imgs/2023/03/07/77c7864368b51c73.png"></p>
<h2 id="模式优缺点"><a href="#模式优缺点" class="headerlink" title="模式优缺点"></a>模式优缺点</h2><ul>
<li>优点<ul>
<li>当创建新的对象实例较为复杂时，使用原型模式可以简化对象的创建过程 ，通过一个已有实例可以提高新实例的创建效率。</li>
</ul>
</li>
<li>缺点<ul>
<li>需要为每一个类配备一个克隆方法 ，而且这个克隆方法需要对类的功能进行通盘考虑，这对全新的类来说不是很难，但对已有的类进行改造时，不一定是件容易的事，必须修改其源代码，违背了“开闭原则”。</li>
<li>在 <strong>实现深克隆时需要编写较为复杂的代码</strong>。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>软件系统设计-建造者模式</title>
    <url>/2023/03/07/%E5%A4%A7%E5%AD%A6/%E8%BD%AF%E7%BB%9F/%E8%BD%AF%E4%BB%B6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1-%E5%BB%BA%E9%80%A0%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="模式动机"><a href="#模式动机" class="headerlink" title="模式动机"></a>模式动机</h2><ul>
<li>软件开发中存在大量复杂对象，<strong>他们拥有一系列成员属性，这些成员属性中有些是引用类型的成员对象</strong>。这些复杂对象还存在一些限制条件。</li>
<li>建造者返还给客户端的是一个已经建造完毕的完整产品对象，而用户无须关心该对象所包含的属性以及它们的组装方式，这就是建造者模式的模式动机。</li>
</ul>
<h2 id="模式定义"><a href="#模式定义" class="headerlink" title="模式定义"></a>模式定义</h2><ul>
<li>将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示。</li>
<li><strong>一步一步创建一个复杂的对象</strong> ，它允许用户只通过指定复杂对象的类型和内容就可以构建它们，用户不需要知道内部的具体构建细节。</li>
<li>建造者模式属于<strong>对象创建型模式</strong>，又可以称为<strong>生成器模式</strong></li>
</ul>
<h2 id="模式结构"><a href="#模式结构" class="headerlink" title="模式结构"></a>模式结构</h2><p><img  src="https://s3.bmp.ovh/imgs/2023/03/07/b3e7751293a60cf3.png"  ><span class="image-caption">image-20230307141744317</span></p>
<ul>
<li>建造者模式包含如下角色：<ul>
<li>Builder: 抽象建造者</li>
<li>ConcreteBuilder: 具体建造者</li>
<li>Director: 指挥者</li>
<li>Product: 产品角色</li>
</ul>
</li>
<li>为什么不能把Builder和Director合并呢?<ul>
<li>单一职责原则</li>
</ul>
</li>
</ul>
<h2 id="模式优缺点"><a href="#模式优缺点" class="headerlink" title="模式优缺点"></a>模式优缺点</h2><ul>
<li>优点<ul>
<li>客户端不必知道产品内部组成的细节，将产品本身与产品的创建过程解耦，使得相同的创建过程可以创建不同的产品对象 。</li>
<li>增加新的具体建造者无须修改原有类库的代码，指挥者类针对抽象建造者类编程，系统扩展方便，符合“开闭原则”。</li>
<li>可以更加精细地控制产品的创建过程。</li>
<li>可以很方便地替换具体建造者或增加新的具体建造者， 用户使用不同的具体建造者即可得到不同的产品对象。</li>
</ul>
</li>
<li>缺点<ul>
<li>建造者模式所创建的产品一般具有较多的共同点，其组成部分相似，如果产品之间的差异性很大，则不适合使用建造者模式，因此其使用范围受到一定的限制 。</li>
<li>如果产品的内部变化复杂，可能会导致需要定义很多具体建造者类来实现这种变化，导致系统变得很庞大 。</li>
</ul>
</li>
</ul>
<h2 id="建造者模式与抽象工厂模式的比较"><a href="#建造者模式与抽象工厂模式的比较" class="headerlink" title="建造者模式与抽象工厂模式的比较"></a>建造者模式与抽象工厂模式的比较</h2><ul>
<li>在抽象工厂模式中， 客户端实例化工厂类，然后调用工厂方法获取所需产品对象 ，而在建造者模式中 客户端可以不直接调用建造者的相关方法，而是通过指挥者类来指导如何生成对象，包括对象的组装过程和建造步骤，它侧重于一步步构造一个复杂对象，返回一个完整的对象。</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>软件系统设计-状态与命令模式</title>
    <url>/2023/03/07/%E5%A4%A7%E5%AD%A6/%E8%BD%AF%E7%BB%9F/%E8%BD%AF%E4%BB%B6%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1-%E7%8A%B6%E6%80%81%E4%B8%8E%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="状态模式"><a href="#状态模式" class="headerlink" title="状态模式"></a>状态模式</h1><h2 id="模式动机"><a href="#模式动机" class="headerlink" title="模式动机"></a>模式动机</h2><ul>
<li><strong>一个对象的行为取决于一个或多个动态变化的属性</strong>，这样的属性叫做状态，这样的对象叫做<strong>有状态的(stateful)对象</strong>，这样的对象状态是从事先定义好的一系列值中取出的。当一个这样的对象与外部事件产生互动时，其内部状态就会改变，从而使得系统的行为也随之发生变化。</li>
</ul>
<h2 id="模式结构"><a href="#模式结构" class="headerlink" title="模式结构"></a>模式结构</h2><p><img src="https://s3.bmp.ovh/imgs/2023/03/07/3e98d2b6c3a15d0c.png"></p>
<ul>
<li><strong>行为型模式</strong>更关注向外暴露的接口，也就是使用的方法</li>
<li>虽然和策略模式的类结构相同，但在行为上不同</li>
<li>状态模式的关键是<strong>引入了一个抽象类来专门表示对象的状态</strong>，这个类我们叫做<strong>抽象状态类</strong>，而对象的每一种具体状态类都继承了该类，并<strong>在不同具体状态类中实现了不同状态的行为，包括各种状态之间的转换。</strong></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库开发03-B树索引的结构和应用</title>
    <url>/2023/03/06/%E5%A4%A7%E5%AD%A6/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%BC%80%E5%8F%9103-B%E6%A0%91%E7%B4%A2%E5%BC%95%E7%9A%84%E7%BB%93%E6%9E%84%E5%92%8C%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<h2 id="构建索引带来的问题"><a href="#构建索引带来的问题" class="headerlink" title="构建索引带来的问题"></a>构建索引带来的问题</h2><ul>
<li>消耗大量物理空间</li>
<li>进行更新和删除操作时开销增大</li>
</ul>
<h2 id="索引降低查询效率的情况"><a href="#索引降低查询效率的情况" class="headerlink" title="索引降低查询效率的情况"></a>索引降低查询效率的情况</h2><ul>
<li>基本表很小</li>
<li>满足同一索引的记录过多，通过索引搜索出的记录过多</li>
</ul>
<h2 id="胖表和瘦表"><a href="#胖表和瘦表" class="headerlink" title="胖表和瘦表"></a>胖表和瘦表</h2><ul>
<li>胖表字段数较多，但记录数较少</li>
<li>瘦表字段数较少，记录数较多</li>
<li>胖表和瘦表的最大区别是单一记录的字段长短不同，一个物理块中存放数据的数量不同</li>
</ul>
<h2 id="什么时候应该使用B树索引"><a href="#什么时候应该使用B树索引" class="headerlink" title="什么时候应该使用B树索引"></a>什么时候应该使用B树索引</h2><ul>
<li>仅需要通过索引访问基本表的很少一部分行</li>
<li>如果要处理表中的多行，可以使用索引而不使用表</li>
</ul>
<h2 id="索引组织表（IOT）"><a href="#索引组织表（IOT）" class="headerlink" title="索引组织表（IOT）"></a>索引组织表（IOT）</h2><ul>
<li>基于整个基本表创建的索引</li>
<li>不更新，但需要通过编码前缀进行快速检索</li>
<li>代码表（如全国省市表）会做成IOT</li>
</ul>
<h2 id="读取索引"><a href="#读取索引" class="headerlink" title="读取索引"></a>读取索引</h2><ul>
<li>索引只是查询工作的第一步</li>
<li>读取基本表中的数据才是查询的结束</li>
<li>同样的索引但不同的物理结构，可能会引起查询效率的千差万别<ul>
<li>磁盘访问的速率</li>
<li>物理I&#x2F;O很可能是内存访问</li>
<li>记录存储</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库开发04-其他类型的索引</title>
    <url>/2023/03/08/%E5%A4%A7%E5%AD%A6/%E6%95%B0%E6%8D%AE%E5%BA%93/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%BC%80%E5%8F%9104-%E5%85%B6%E4%BB%96%E7%B1%BB%E5%9E%8B%E7%9A%84%E7%B4%A2%E5%BC%95/</url>
    <content><![CDATA[<h2 id="哈希索引-Hash-Index"><a href="#哈希索引-Hash-Index" class="headerlink" title="哈希索引(Hash Index)"></a>哈希索引(Hash Index)</h2><ul>
<li>MySQL</li>
<li>使用Hash函数建立索引</li>
<li>能支持<ul>
<li>全键值匹配</li>
<li>只能进行等于，不等于等匹配</li>
<li>作全键值匹配时效率远远大于B树索引</li>
</ul>
</li>
<li>不支持<ul>
<li>范围匹配，模糊匹配</li>
<li>存在碰撞率的问题</li>
</ul>
</li>
</ul>
<h2 id="位图索引-Bitmap-Index"><a href="#位图索引-Bitmap-Index" class="headerlink" title="位图索引(Bitmap Index)"></a>位图索引(Bitmap Index)</h2><ul>
<li>Oracle</li>
</ul>
<p><img src="https://s3.bmp.ovh/imgs/2023/03/08/30a6334dc139216a.png"></p>
<ul>
<li>压缩程度高，紧凑度高，可能只有一两个Block</li>
<li>只要产生update等操作，会导致整个block锁住，并行写效率极低</li>
<li>什么时候用<ul>
<li>相异基数低</li>
<li>大量临时查询的聚合</li>
</ul>
</li>
</ul>
<h2 id="位图联结索引-bitmap-join-index"><a href="#位图联结索引-bitmap-join-index" class="headerlink" title="位图联结索引(bitmap join index)"></a>位图联结索引(bitmap join index)</h2><ul>
<li><p>允许使用另外某个表的列对一个给定表建立索引。</p>
</li>
<li><p>实际上，这就是允许对一个索引结构（而不是表本身）中的数据进行逆规范化。</p>
</li>
<li><p>MySQL没有位图索引</p>
<ul>
<li>优化替代索引组合</li>
<li>低选择性添加特殊索引</li>
</ul>
</li>
</ul>
<h2 id="函数索引-function-based-index"><a href="#函数索引-function-based-index" class="headerlink" title="函数索引(function-based index)"></a>函数索引(function-based index)</h2><ul>
<li>对F(x)的值构建索引，再通过对索引读取x所指向的记录行</li>
<li>用在哪？<ul>
<li>不区分大小写的索引（通过函数把内容全变成大写或小写，减少查询压力）</li>
<li>true和false值存在巨大差异下的索引（如true过多，可以只把false放到索引中，通过函数把true过滤掉，可以减小index的block大小</li>
<li>有选择的唯一性</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Java-集合源码阅读-接口篇</title>
    <url>/2023/03/09/Java/Java-%E9%9B%86%E5%90%88%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-%E6%8E%A5%E5%8F%A3%E7%AF%87/</url>
    <content><![CDATA[<p><img src="https://s3.bmp.ovh/imgs/2023/03/09/02507300fff3dde9.png"></p>
<h2 id="Collection接口"><a href="#Collection接口" class="headerlink" title="Collection接口"></a>Collection接口</h2><h3 id="接口描述"><a href="#接口描述" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	集合层次结构中的根接口。集合表示一组对象。有些集合允许重复元素，有些则不允许。 有些是有序的，有些是无序的。</p>
<p>​	包或多重集（可能包含重复元素的无序集合）应直接实现此接口。所有通用 Collection 实现类（通常通过其子接口之一间接实现 Collection）应<strong>提供两个“标准”构造函数：一个 void（无参数）构造函数，它创建一个空集合，以及一个具有单个类型参数的构造函数 Collection，它创建一个新的集合，其元素与其参数相同。</strong> 实际上，后一个构造函数允许用户复制任何集合，从而生成所需实现类型的等效集合。 没有办法强制执行此约定（因为接口不能包含构造函数）但 Java 平台库中的所有通用 Collection 实现都遵守。</p>
<p>​	如果在<strong>不支持该操作的集合上使用某些方法</strong>，可能会导致抛出<code>UnsupportedOperationException</code> 异常。</p>
<p>​	尝试<strong>添加不合格的元素</strong>会引发未经检查的异常，通常是 <code>NullPointerException</code> 或 <code>ClassCastException</code>。 </p>
<p>​	尝试<strong>查询不合格元素的存在</strong>可能会抛出异常，或者可能只是返回 false。</p>
<p>​	尝试<strong>对不合格元素执行操作，其完成不会导致将不合格元素插入集合中</strong>可能会抛出异常，也可能会成功，具体取决于实现的选择。</p>
<h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回集合中的元素数量，如果集合中的元素数大于Integer.MAX_VALUE,返回Integer.MAX_VALUE</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>; <br><br><span class="hljs-comment">//返回集合中是否存在元素</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果集合中包含该特定元素就返回true</span><br><span class="hljs-comment">//具体的实现方式为：如果集合中包含至少一个元素e，return (o==null ? e==null : o.equals(e));</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回该集合中元素的一个迭代器</span><br>Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//此集合不维护对新建的数组的引用，也就是分配一个新数组，调用者可以自由返回的数组</span><br><span class="hljs-comment">//此方法充当基于数组和基于集合的API之间的桥梁</span><br>Object[] toArray();<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//返回数组的运行时类型是指定数组的类型</span><br><span class="hljs-comment">//如果集合适合指定的数组，则在其中返回。 否则，将使用指定数组的运行时类型和此集合的大小分配一个新数组</span><br><span class="hljs-comment">//如果此集合适合指定的数组并有剩余空间（即，数组的元素多于此集合），则紧跟在集合末尾的数组中的元素将设置为 null。</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//String[] y = x.toArray(new String[0]);</span><br>&lt;T&gt; T[] toArray(T[] a);<br><br><span class="hljs-comment">//如果此集合因为此调用而更改，则返回true</span><br><span class="hljs-comment">//此集合不允许重复且已包含指定元素则返回false</span><br><span class="hljs-comment">//如果集合出于任何原因拒绝添加特定元素，而不是因为它已经包含该元素，则它必须抛出异常（而不是返回 false）</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//从集合中删除特定元素的一个实例</span><br><span class="hljs-comment">//如果此集合包含指定元素，则返回 true（或者等效地，如果此集合因调用而更改）</span><br><span class="hljs-comment">//如果有多个元素o，只会移除第一个</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">remove</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//如果该集合包含传入集合中的所有元素，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将指定集合中的所有元素添加到此集合（可选操作）</span><br><span class="hljs-comment">//如果指定集合在进行时被修改，则该操作是未定义的</span><br><span class="hljs-comment">//最常见的就是调用addAll时传入自己作为参数</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span>;<br><br><span class="hljs-comment">//删除此集合的所有也包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//此调用返回后，此集合将不包含与指定集合共有的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//移除此集合中满足给定谓词的所有元素。 </span><br><span class="hljs-comment">//在迭代期间或由谓词抛出的错误或运行时异常被转发给调用者。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-keyword">default</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">removeIf</span><span class="hljs-params">(Predicate&lt;? <span class="hljs-built_in">super</span> E&gt; filter)</span> &#123;<br>   Objects.requireNonNull(filter);<br>   <span class="hljs-type">boolean</span> <span class="hljs-variable">removed</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>   <span class="hljs-keyword">final</span> Iterator&lt;E&gt; each = iterator();<br>   <span class="hljs-keyword">while</span> (each.hasNext()) &#123;<br>       <span class="hljs-keyword">if</span> (filter.test(each.next())) &#123;<br>           each.remove();<br>           removed = <span class="hljs-literal">true</span>;<br>       &#125;<br>   &#125;<br>   <span class="hljs-keyword">return</span> removed;<br>&#125;<br><br><span class="hljs-comment">//仅保留此集合中包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//换句话说，从该集合中移除所有未包含在指定集合中的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">retainAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将该集合中所有元素都移除</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">clear</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//比较指定对象与此集合是否相等</span><br><span class="hljs-comment">//Object.equals 方法的一般约定规定 equals 必须是对称的（换句话说，a.equals(b) 当且仅当 b.equals(a) 时）</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回此集合的哈希码值</span><br><span class="hljs-comment">//任何重写 Object.equals 方法的类也必须重写 Object.hashCode 方法以满足 Object 的通用契约 .hashCode 方法</span><br><span class="hljs-comment">//特别是，c1.equals(c2) 意味着 c1.hashCode()==c2.hashCode()</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure>

<h2 id="Set接口"><a href="#Set接口" class="headerlink" title="Set接口"></a>Set接口</h2><p>继承 collection接口</p>
<h3 id="接口描述-1"><a href="#接口描述-1" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	集合<strong>不包含满足 e1.equals(e2) 的一对元素 e1 和 e2，并且最多包含一个空元素</strong>。</p>
<p>​	集合<strong>不允许将自身作为元素包含</strong>。</p>
<h3 id="方法-1"><a href="#方法-1" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回集合中的元素数量，如果集合中的元素数大于Integer.MAX_VALUE,返回Integer.MAX_VALUE</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>; <br><br><span class="hljs-comment">//返回集合中是否存在元素</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果集合中包含该特定元素就返回true</span><br><span class="hljs-comment">//具体的实现方式为：如果集合中包含至少一个元素e，return (o==null ? e==null : o.equals(e));</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回该集合中元素的一个迭代器</span><br>Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//此集合不维护对新建的数组的引用，也就是分配一个新数组，调用者可以自由返回的数组</span><br><span class="hljs-comment">//此方法充当基于数组和基于集合的API之间的桥梁</span><br>Object[] toArray();<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//返回数组的运行时类型是指定数组的类型</span><br><span class="hljs-comment">//如果集合适合指定的数组，则在其中返回。 否则，将使用指定数组的运行时类型和此集合的大小分配一个新数组</span><br><span class="hljs-comment">//如果此集合适合指定的数组并有剩余空间（即，数组的元素多于此集合），则紧跟在集合末尾的数组中的元素将设置为 null。</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//String[] y = x.toArray(new String[0]);</span><br>&lt;T&gt; T[] toArray(T[] a);<br><br><span class="hljs-comment">//如果此集合因为此调用而更改，则返回true</span><br><span class="hljs-comment">//此集合不允许重复且已包含指定元素则返回false</span><br><span class="hljs-comment">//如果集合出于任何原因拒绝添加特定元素，而不是因为它已经包含该元素，则它必须抛出异常（而不是返回 false）</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//从集合中删除特定元素的一个实例</span><br><span class="hljs-comment">//如果此集合包含指定元素，则返回 true（或者等效地，如果此集合因调用而更改）</span><br><span class="hljs-comment">//如果有多个元素o，只会移除第一个</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">remove</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//如果该集合包含传入集合中的所有元素，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将指定集合中的所有元素添加到此集合（可选操作）</span><br><span class="hljs-comment">//如果指定集合在进行时被修改，则该操作是未定义的</span><br><span class="hljs-comment">//最常见的就是调用addAll时传入自己作为参数</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span>;<br><br><span class="hljs-comment">//删除此集合的所有也包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//此调用返回后，此集合将不包含与指定集合共有的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//移除此集合中满足给定谓词的所有元素。 </span><br><span class="hljs-comment">//在迭代期间或由谓词抛出的错误或运行时异常被转发给调用者。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-keyword">default</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">removeIf</span><span class="hljs-params">(Predicate&lt;? <span class="hljs-built_in">super</span> E&gt; filter)</span> &#123;<br>   Objects.requireNonNull(filter);<br>   <span class="hljs-type">boolean</span> <span class="hljs-variable">removed</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>   <span class="hljs-keyword">final</span> Iterator&lt;E&gt; each = iterator();<br>   <span class="hljs-keyword">while</span> (each.hasNext()) &#123;<br>       <span class="hljs-keyword">if</span> (filter.test(each.next())) &#123;<br>           each.remove();<br>           removed = <span class="hljs-literal">true</span>;<br>       &#125;<br>   &#125;<br>   <span class="hljs-keyword">return</span> removed;<br>&#125;<br><br><span class="hljs-comment">//仅保留此集合中包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//换句话说，从该集合中移除所有未包含在指定集合中的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">retainAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将该集合中所有元素都移除</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">clear</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果指定的对象也是一个集合，两个集合的大小相同，并且指定集合的每个成员都包含在这个集合中则返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回此集合的哈希码值</span><br><span class="hljs-comment">//任何重写 Object.equals 方法的类也必须重写 Object.hashCode 方法以满足 Object 的通用契约 .hashCode 方法</span><br><span class="hljs-comment">//特别是，c1.equals(c2) 意味着 c1.hashCode()==c2.hashCode()</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>add()方法中set可以选择不接受所有元素并抛出异常</li>
</ul>
<h2 id="SortedSet接口"><a href="#SortedSet接口" class="headerlink" title="SortedSet接口"></a>SortedSet接口</h2><p>继承set接口</p>
<h3 id="接口描述-2"><a href="#接口描述-2" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	相较于set，SortedSet为他的所有元素提供了一个整体上的排序。元素根据他们的自然顺序进行排序，或者通过在排序集创建时提供的比较器进行排序。集合的迭代器将按照升序元素的顺序遍历集合。</p>
<p>​	所有插入有序集合的元素必须实现Comparable接口。</p>
<p>​	所有通用排序集实现类都应提供四个“标准”构造函数：</p>
<pre><code>1) void（无参数）构造函数，它创建一个空的排序集，该排序集根据其元素的自然顺序排序。 
2) 具有 Comparator 类型的单个参数的构造函数，它创建根据指定比较器排序的空排序集。 
3) 具有单个 Collection 类型参数的构造函数，它创建一个新的有序集合，其元素与其参数相同，并根据元素的自然顺序进行排序。 4) 
4) 一个带有单个 SortedSet 类型参数的构造函数，它创建一个新的有序集合，它具有与输入有序集合相同的元素和相同的顺序。
</code></pre>
<h3 id="方法-2"><a href="#方法-2" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回用于对该集合中的元素进行排序的比较器</span><br><span class="hljs-comment">//如果该集合使用其元素的自然排序，则返回 null</span><br>Comparator&lt;? <span class="hljs-built_in">super</span> E&gt; comparator();<br><br><span class="hljs-comment">//返回该顺序集的部分视图</span><br>SortedSet&lt;E&gt; <span class="hljs-title function_">subSet</span><span class="hljs-params">(E fromElement, E toElement)</span>;<br><br><span class="hljs-comment">//返回此集合中其元素严格小于 toElement 的部分的视图</span><br>SortedSet&lt;E&gt; <span class="hljs-title function_">headSet</span><span class="hljs-params">(E toElement)</span>;<br><br><span class="hljs-comment">//返回此集合中其元素大于或等于 fromElement 的部分的视图</span><br>SortedSet&lt;E&gt; <span class="hljs-title function_">tailSet</span><span class="hljs-params">(E fromElement)</span>;<br><br><span class="hljs-comment">//返回当前集合中的最小元素</span><br>E <span class="hljs-title function_">first</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回当前集合中的最大元素</span><br>E <span class="hljs-title function_">last</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure>



<h2 id="List接口"><a href="#List接口" class="headerlink" title="List接口"></a>List接口</h2><p>继承collection接口</p>
<h3 id="接口描述-3"><a href="#接口描述-3" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	有序集合（也称为序列）。 该界面的用户可以精确控制每个元素在列表中的插入位置。 用户可以通过整数索引（在列表中的位置）访问元素，并在列表中搜索元素。</p>
<p>​	与集合不同，列表通常允许重复元素。 更正式地说，列表通常允许成对的元素 e1 和 e2，例如 e1.equals(e2)，并且如果它们完全允许 null 元素，则它们通常允许多个 null 元素。</p>
<p>​	List 接口在<strong>迭代器、add、remove、equals 和 hashCode 方法</strong>的约定上放置了额外的规定，超出了 Collection 接口中指定的规定。</p>
<p>​	List 接口提供了<strong>四种</strong>用于对列表元素进行位置（索引）访问的方法。</p>
<p>​	List 接口提供了一个特殊的迭代器，称为 ListIterator，除了 Iterator 接口提供的正常操作之外，它还允许元素插入和替换以及双向访问。 提供了一种方法来获取从列表中的指定位置开始的列表迭代器。</p>
<p>​	List 接口提供了<strong>两种方法</strong>来搜索指定的对象。 从性能的角度来看，应谨慎使用这些方法。 在许多实现中，它们将执行代价高昂的<strong>线性搜索</strong>。</p>
<p>​	List 接口提供了<strong>两种方法</strong>来高效地在列表中的任意点插入和删除多个元素。</p>
<p>​	对List进行<strong>equals</strong>和<strong>hashCode</strong>方法并不能得到理想的返回值</p>
<h3 id="方法-3"><a href="#方法-3" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回集合中的元素数量，如果集合中的元素数大于Integer.MAX_VALUE,返回Integer.MAX_VALUE</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>; <br><br><span class="hljs-comment">//返回集合中是否存在元素</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果集合中包含该特定元素就返回true</span><br><span class="hljs-comment">//具体的实现方式为：如果集合中包含至少一个元素e，return (o==null ? e==null : o.equals(e));</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">contains</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回该集合中元素的一个迭代器</span><br>Iterator&lt;E&gt; <span class="hljs-title function_">iterator</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//此集合不维护对新建的数组的引用，也就是分配一个新数组，调用者可以自由返回的数组</span><br><span class="hljs-comment">//此方法充当基于数组和基于集合的API之间的桥梁</span><br>Object[] toArray();<br><br><span class="hljs-comment">//返回一个包含所有在集合中的元素的数组</span><br><span class="hljs-comment">//返回数组的运行时类型是指定数组的类型</span><br><span class="hljs-comment">//如果集合适合指定的数组，则在其中返回。 否则，将使用指定数组的运行时类型和此集合的大小分配一个新数组</span><br><span class="hljs-comment">//如果此集合适合指定的数组并有剩余空间（即，数组的元素多于此集合），则紧跟在集合末尾的数组中的元素将设置为 null。</span><br><span class="hljs-comment">//如果这个集合对其迭代器返回其元素的顺序做出保证，则该方法必须以相同的顺序返回元素</span><br><span class="hljs-comment">//String[] y = x.toArray(new String[0]);</span><br>&lt;T&gt; T[] toArray(T[] a);<br><br><span class="hljs-comment">//如果此集合因为此调用而更改，则返回true</span><br><span class="hljs-comment">//此集合不允许重复且已包含指定元素则返回false</span><br><span class="hljs-comment">//如果集合出于任何原因拒绝添加特定元素，而不是因为它已经包含该元素，则它必须抛出异常（而不是返回 false）</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//从集合中删除特定元素的一个实例</span><br><span class="hljs-comment">//如果此集合包含指定元素，则返回 true（或者等效地，如果此集合因调用而更改）</span><br><span class="hljs-comment">//如果有多个元素o，只会移除第一个</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">remove</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//如果该集合包含传入集合中的所有元素，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将指定集合中的所有元素添加到此集合（可选操作）</span><br><span class="hljs-comment">//如果指定集合在进行时被修改，则该操作是未定义的</span><br><span class="hljs-comment">//最常见的就是调用addAll时传入自己作为参数</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span>;<br><br><span class="hljs-comment">//将指定集合中的所有元素插入此列表的指定位置</span><br><span class="hljs-comment">//将当前位于该位置的元素（如果有）和任何后续元素向右移动（增加它们的索引）。 </span><br><span class="hljs-comment">//新元素将按照指定集合的迭代器返回的顺序出现在该列表中。 </span><br><span class="hljs-comment">//如果在操作进行时修改了指定的集合，则此操作的行为是未定义的。</span><br><span class="hljs-comment">//最常见的就是调用addAll时传入自己作为参数</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">addAll</span><span class="hljs-params">(<span class="hljs-type">int</span> index, Collection&lt;? extends E&gt; c)</span>;<br><br><span class="hljs-comment">//删除此集合的所有也包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//此调用返回后，此集合将不包含与指定集合共有的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//仅保留此集合中包含在指定集合中的元素（可选操作）。 </span><br><span class="hljs-comment">//换句话说，从该集合中移除所有未包含在指定集合中的元素。</span><br><span class="hljs-comment">//如果集合发生改变，返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">retainAll</span><span class="hljs-params">(Collection&lt;?&gt; c)</span>;<br><br><span class="hljs-comment">//将此列表的每个元素替换为对该元素应用运算符的结果。</span><br><span class="hljs-comment">//给出一些operator的例子</span><br><span class="hljs-comment">//x -&gt; x + 1;  //list里面所有的数值自增1，其中1可以改成任意数字</span><br><span class="hljs-comment">//x -&gt; x - 1; //list里面所有的数值自减1，其中1可以改成任意数字</span><br><span class="hljs-comment">//x -&gt; x++; //list里面所有的数值自增</span><br><span class="hljs-comment">//x -&gt; ++x; //list里面所有的数值自增 （这里 ++x 和 x++ 的区别想必大家都知道，我就不说了）</span><br><span class="hljs-comment">//x -&gt; x--; //list里面所有的数值自减</span><br><span class="hljs-comment">//x -&gt; --x; //list里面所有的数值自减</span><br><span class="hljs-comment">//x -&gt; ~x; //list里面所有的数值变成相反数，即正数变为负数，负数变为正数</span><br><span class="hljs-comment">//x -&gt; x * 2; //list里面所有的数值乘以2，同样的2也是可以改成任意数字</span><br><span class="hljs-comment">//x -&gt; x / 2; //list所有的数都变成除以2之后的数，2可变</span><br><span class="hljs-comment">//x -&gt; x % 2; //list所有的数都变成除以2之后的余数，2可变</span><br><span class="hljs-comment">//p.replaceAll(t-&gt;t+1);</span><br><span class="hljs-keyword">default</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">replaceAll</span><span class="hljs-params">(UnaryOperator&lt;E&gt; operator)</span>;<br><br><span class="hljs-comment">//根据指定的 Comparator 引起的顺序对该列表进行排序</span><br><span class="hljs-keyword">default</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sort</span><span class="hljs-params">(Comparator&lt;? <span class="hljs-built_in">super</span> E&gt; c)</span>;<br><br><span class="hljs-comment">//将该集合中所有元素都移除</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">clear</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果两个列表包含相同顺序的相同元素，则它们被定义为相等</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回此集合的哈希码值</span><br><span class="hljs-comment">//任何重写 Object.equals 方法的类也必须重写 Object.hashCode 方法以满足 Object 的通用契约 .hashCode 方法</span><br><span class="hljs-comment">//特别是，c1.equals(c2) 意味着 c1.hashCode()==c2.hashCode()</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回list指定index处的元素</span><br>E <span class="hljs-title function_">get</span><span class="hljs-params">(<span class="hljs-type">int</span> index)</span>;<br><br><span class="hljs-comment">//将list指定index处的元素替换为指定元素</span><br>E <span class="hljs-title function_">set</span><span class="hljs-params">(<span class="hljs-type">int</span> index, E element)</span>;<br><br><span class="hljs-comment">//在此列表中的指定位置插入指定元素（可选操作）。 </span><br><span class="hljs-comment">//将当前位于该位置的元素（如果有）和任何后续元素向右移动（将其索引加一）。</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(<span class="hljs-type">int</span> index, E element)</span>;<br><br><span class="hljs-comment">//删除此列表中指定位置的元素（可选操作）。 </span><br><span class="hljs-comment">//将任何后续元素向左移动（从其索引中减去一个）。 返回从列表中删除的元素。</span><br>E <span class="hljs-title function_">remove</span><span class="hljs-params">(<span class="hljs-type">int</span> index)</span>;<br><br><span class="hljs-comment">//返回此列表中指定元素第一次出现的索引，如果此列表不包含该元素，则返回 -1。</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">indexOf</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回此列表中指定元素最后一次出现的索引，如果此列表不包含该元素，则返回 -1。</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">lastIndexOf</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//返回该list元素上的一个list迭代器</span><br>ListIterator&lt;E&gt; <span class="hljs-title function_">listIterator</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此列表中元素的列表迭代器（以适当的顺序），从列表中的指定位置开始</span><br><span class="hljs-comment">//对该迭代器首次调用next将返回指定index的元素</span><br><span class="hljs-comment">//对该迭代器首次调用previous将返回指定index-1的元素</span><br>ListIterator&lt;E&gt; <span class="hljs-title function_">listIterator</span><span class="hljs-params">(<span class="hljs-type">int</span> index)</span>;<br><br><span class="hljs-comment">//返回从from到toindex的一个子视图，对该视图进行的修改操作会直接影响到原列表，反之亦然</span><br>List&lt;E&gt; <span class="hljs-title function_">subList</span><span class="hljs-params">(<span class="hljs-type">int</span> fromIndex, <span class="hljs-type">int</span> toIndex)</span>;<br></code></pre></td></tr></table></figure>

<h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><ul>
<li>多了一个<code>boolean addAll(int index, Collection&lt;? extends E&gt; c);</code>方法，用于在指定index处addall</li>
<li><code>default void replaceAll(UnaryOperator&lt;E&gt; operator);</code>方法允许对list中所有元素进行统一的变换</li>
<li>必须list中每个位置的元素相同，equal()才返回true</li>
<li><code>ListIterator&lt;E&gt; listIterator();</code>是list专用的迭代器，同时允许获取指定index处的迭代器</li>
<li>get(index),set(index,e),add(index,e)</li>
<li>subList视图</li>
</ul>
<h2 id="Queue接口"><a href="#Queue接口" class="headerlink" title="Queue接口"></a>Queue接口</h2><p>继承collection接口</p>
<h3 id="接口描述-4"><a href="#接口描述-4" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	设计用于在处理之前保存元素的集合。 除了基本的 Collection 操作外，队列还提供<strong>额外的插入、提取和检查操作</strong>。 这些方法中的<strong>每一个都以两种形式</strong>存在：一种在操作失败时<strong>抛出异常</strong>，另一种<strong>返回一个特殊值</strong>（null 或 false，具体取决于操作）。 后一种形式的插入操作专门设计用于容量受限的 Queue 实现； 在大多数实现中，插入操作不会失败。</p>
<table>
<thead>
<tr>
<th align="center">方法作用\特殊操作</th>
<th align="center">抛出异常</th>
<th align="center">返回特殊值</th>
</tr>
</thead>
<tbody><tr>
<td align="center">插入</td>
<td align="center">add(e)</td>
<td align="center">offer(e)</td>
</tr>
<tr>
<td align="center">删除</td>
<td align="center">remove()</td>
<td align="center">poll()</td>
</tr>
<tr>
<td align="center">检查（一般是第一个）元素</td>
<td align="center">element()</td>
<td align="center">peek()</td>
</tr>
</tbody></table>
<p>​	队列通常（但不一定）以 FIFO（先进先出）方式对元素进行排序。 其中的例外是<strong>优先级队列</strong>，它根据提供的比较器或元素的自然顺序对元素进行排序，以及对元素进行 LIFO（后进先出）排序的 <strong>LIFO 队列（或堆栈）</strong>。 </p>
<p>​	无论使用何种顺序，通过调用 remove() 或 poll() 将删除队列的头部元素。 在 FIFO 队列中，所有新元素都插入到队列的尾部。 其他类型的队列可能使用不同的放置规则。 每个 Queue 实现都必须指定其排序属性。</p>
<p>​	如果可能，<strong>offer 方法</strong>将插入一个元素，否则返回 false。offer 方法设计用于<strong>故障是正常情况而不是异常情况</strong>，例如，在固定容量（或“有界”）队列中。</p>
<p>​	<strong>remove() 和 poll() 方法</strong>移除并返回队列的头部。remove() 和 poll() 方法仅在队列为空时的行为不同：<strong>remove() 方法抛出异常，而 poll() 方法返回 null</strong>。</p>
<p>​	<strong>element() 和 peek() 方法</strong>返回但<strong>不移除</strong>队列的头部。</p>
<p>​	队列实现<strong>通常不允许</strong>插入空元素，尽管某些实现（例如 LinkedList）<strong>不禁止插入空元素</strong>。 即使在允许它的实现中，也不应将 null 插入到队列中，因为 null 也被 poll 方法用作特殊的返回值，以指示队列不包含任何元素。</p>
<p>​	队列实现通常不定义方法 <strong>equals 和 hashCode</strong> 的基于元素的版本，而是从类 Object 继承基于身份的版本，因为对于具有<strong>相同元素但排序属性不同</strong>的队列，<strong>基于元素的相等并不总是定义明确</strong>。</p>
<h3 id="方法-4"><a href="#方法-4" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//将指定元素插入队列，成功时返回true</span><br><span class="hljs-comment">//若出错（如没有可用空间）则抛出IllegalStateException</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//将指定元素插入队列，成功时返回true</span><br><span class="hljs-comment">//若出错（如没有可用空间）则返回false而不是抛出异常</span><br><span class="hljs-comment">//更适合用来插入容量受限的队列</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">offer</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//返回并删除此队列的头部</span><br><span class="hljs-comment">//如果此队列为空会抛出异常</span><br>E <span class="hljs-title function_">remove</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回并删除此队列的头部</span><br><span class="hljs-comment">//如果此队列为空会返回null</span><br>E <span class="hljs-title function_">poll</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回但不删除队列的头部</span><br><span class="hljs-comment">//如果队列为空会返回异常</span><br>E <span class="hljs-title function_">element</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回但不删除队列的头部</span><br><span class="hljs-comment">//如果队列为空会返回异null</span><br>E <span class="hljs-title function_">peek</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure>

<h2 id="Deque接口"><a href="#Deque接口" class="headerlink" title="Deque接口"></a>Deque接口</h2><p>继承queue接口</p>
<h3 id="接口描述-5"><a href="#接口描述-5" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	支持两端元素插入和移除的线性集合。</p>
<p>​	该接口定义了访问双端队列两端元素的方法。 提供了插入、删除和检查元素的方法。 这些方法中的每一个都以<strong>两种形式</strong>存在：一种在操作失败时抛出异常，另一种返回一个特殊值（null 或 false，具体取决于操作）。 后一种形式的插入操作专门设计用于容量受限的 Deque 实现。</p>
<table>
<thead>
<tr>
<th align="center">操作&#x2F;类型</th>
<th align="center">抛出异常</th>
<th align="center">返回特殊值</th>
<th align="center">抛出异常</th>
<th align="center">返回特殊值</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Insert</td>
<td align="center">addFirst(e)</td>
<td align="center">offerFirst(e)</td>
<td align="center">addLast(e)</td>
<td align="center">offerLast(e)</td>
</tr>
<tr>
<td align="center">Remove</td>
<td align="center">removeFirst()</td>
<td align="center">pollFirst()</td>
<td align="center">removeLast()</td>
<td align="center">pollLast()</td>
</tr>
<tr>
<td align="center">Examine</td>
<td align="center">getFirst()</td>
<td align="center">peekFirst()</td>
<td align="center">getLast()</td>
<td align="center">peekLast()</td>
</tr>
</tbody></table>
<h3 id="方法-5"><a href="#方法-5" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//如果可以在不违反容量限制的情况下立即将指定的元素插入此双端队列的前面</span><br><span class="hljs-comment">//没有可用空间时抛出异常</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">addFirst</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//如果可以在不违反容量限制的情况下立即插入指定的元素，则在此双端队列的末尾插入指定的元素</span><br><span class="hljs-comment">//没有可用空间时抛出异常</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">addLast</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//将指定的元素插入此双端队列的前面，除非它违反容量限制</span><br><span class="hljs-comment">//如果成功插入，则返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">offerFirst</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//将指定的元素插入此双端队列的后面，除非它违反容量限制</span><br><span class="hljs-comment">//如果成功插入，则返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">offerLast</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//检索并删除此双端队列的第一个元素</span><br><span class="hljs-comment">//如果队列为空抛出异常</span><br>E <span class="hljs-title function_">removeFirst</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索并删除此双端队列的最后一个元素</span><br><span class="hljs-comment">//如果队列为空抛出异常</span><br>E <span class="hljs-title function_">removeLast</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索并删除此双端队列的第一个元素</span><br><span class="hljs-comment">//如果队列为空则返回null</span><br>E <span class="hljs-title function_">pollFirst</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索并删除此双端队列的最后一个元素</span><br><span class="hljs-comment">//如果队列为空则返回null</span><br>E <span class="hljs-title function_">pollLast</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索但不删除此双端队列的第一个元素</span><br><span class="hljs-comment">//如果队列为空抛出异常</span><br>E <span class="hljs-title function_">getFirst</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索但不删除此双端队列的最后一个元素</span><br><span class="hljs-comment">//如果队列为空抛出异常</span><br>E <span class="hljs-title function_">getLast</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索但不删除此双端队列的第一个元素</span><br><span class="hljs-comment">//如果队列为空则返回null</span><br>E <span class="hljs-title function_">peekFirst</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//检索但不删除此双端队列的最后一个元素</span><br><span class="hljs-comment">//如果队列为空则返回null</span><br>E <span class="hljs-title function_">peekLast</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//从此双端队列中移除第一次出现的指定元素。 如果双端队列不包含该元素，则它不变。</span><br><span class="hljs-comment">//如果成功移除则返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeFirstOccurrence</span><span class="hljs-params">(Object o)</span>;<br><br><span class="hljs-comment">//从此双端队列中移除最后一次出现的指定元素。 如果双端队列不包含该元素，则它不变。</span><br><span class="hljs-comment">//如果成功移除则返回true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">removeLastOccurrence</span><span class="hljs-params">(Object o)</span>;<br><br><br><span class="hljs-comment">/* 队列的方法 */</span><br><br><span class="hljs-comment">//将元素添加到队列最后</span><br><span class="hljs-comment">//会抛出异常</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//将元素添加到队列最后</span><br><span class="hljs-comment">//会返回null值</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">offer</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//获取队列头元素并删除</span><br><span class="hljs-comment">//抛出异常</span><br>E <span class="hljs-title function_">remove</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//获取队列头元素并删除</span><br><span class="hljs-comment">//返回null值</span><br>E <span class="hljs-title function_">poll</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//获取队列头元素不删除</span><br><span class="hljs-comment">//抛出异常</span><br>E <span class="hljs-title function_">element</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//获取队列头元素不删除</span><br><span class="hljs-comment">//返回null值</span><br>E <span class="hljs-title function_">peek</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">/* 栈的方法 */</span><br><br><span class="hljs-comment">//将元素压到栈顶</span><br><span class="hljs-comment">//会抛出异常</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">push</span><span class="hljs-params">(E e)</span>;<br><br><span class="hljs-comment">//将栈顶元素弹出</span><br><span class="hljs-comment">//会抛出异常</span><br>E <span class="hljs-title function_">pop</span><span class="hljs-params">()</span>;<br></code></pre></td></tr></table></figure>

<h3 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h3><p>就是支持两端插入的队列，为实现stack和queue提供方法</p>
<h2 id="Map接口"><a href="#Map接口" class="headerlink" title="Map接口"></a>Map接口</h2><h3 id="接口描述-6"><a href="#接口描述-6" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	将键映射到值的对象，Map不能包含重复的键，一个键最多映射到一个值，</p>
<p>​	所有通用映射实现类都应提供两个“标准”构造函数：一个创建空映射的 void（无参数）构造函数，以及一个具有单个 Map 类型参数的构造函数，该构造函数创建具有相同键值的新映射 映射作为它的参数。</p>
<h3 id="方法-6"><a href="#方法-6" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回集合中的元素数量，如果集合中的元素数大于Integer.MAX_VALUE,返回Integer.MAX_VALUE</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">size</span><span class="hljs-params">()</span>; <br><br><span class="hljs-comment">//返回集合中是否存在元素</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">isEmpty</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//如果此映射包含指定键的映射，则返回 true</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsKey</span><span class="hljs-params">(Object key)</span>;<br><br><span class="hljs-comment">//如果此映射将一个或多个键映射到指定值，则返回 true。</span><br><span class="hljs-comment">//此操作可能需要与地图大小成线性关系的时间</span><br><span class="hljs-type">boolean</span> <span class="hljs-title function_">containsValue</span><span class="hljs-params">(Object value)</span>;<br><br><span class="hljs-comment">//返回指定键映射到的值，如果此映射不包含键的映射，则返回 null。</span><br><span class="hljs-comment">//如果此映射允许空值，则返回值 null 不一定表示该映射不包含键的映射</span><br><span class="hljs-comment">//containsKey 操作可用于区分这两种情况</span><br>V <span class="hljs-title function_">get</span><span class="hljs-params">(Object key)</span>;<br><br><span class="hljs-comment">//将指定值与此映射中的指定键相关联</span><br><span class="hljs-comment">//如果映射先前包含键的映射，则旧值将替换为指定值</span><br>V <span class="hljs-title function_">put</span><span class="hljs-params">(K key, V value)</span>;<br><br><span class="hljs-comment">//如果存在，则从此映射中删除键的映射</span><br><span class="hljs-comment">//返回此映射先前将键关联到的值，如果映射不包含键的映射，则返回 null。</span><br><span class="hljs-comment">//如果此映射允许空值，则返回值 null 不一定表示该映射不包含键的映射</span><br>V <span class="hljs-title function_">remove</span><span class="hljs-params">(Object key)</span>;<br><br><span class="hljs-comment">//将指定映射中的所有映射复制到此映射</span><br><span class="hljs-comment">//等同于为指定映射中从键 k 到值 v 的每个映射在此映射上调用一次 put(k, v) 的效果</span><br><span class="hljs-comment">//如果指定的Map在操作过程中被修改，则此操作的行为是未定义的</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">putAll</span><span class="hljs-params">(Map&lt;? extends K, ? extends V&gt; m)</span>;<br><br><span class="hljs-comment">//删除所有的键值对</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">clear</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的键的 Set 视图</span><br><span class="hljs-comment">//它不支持 add 或 addAll 操作</span><br>Set&lt;K&gt; <span class="hljs-title function_">keySet</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的值的集合视图</span><br><span class="hljs-comment">//它不支持 add 或 addAll 操作。</span><br>Collection&lt;V&gt; <span class="hljs-title function_">values</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的映射的集合视图</span><br>Set&lt;Map.Entry&lt;K, V&gt;&gt; entrySet();<br><br></code></pre></td></tr></table></figure>

<h2 id="SortedMap"><a href="#SortedMap" class="headerlink" title="SortedMap"></a>SortedMap</h2><p>继承Map接口</p>
<h3 id="接口描述-7"><a href="#接口描述-7" class="headerlink" title="接口描述"></a>接口描述</h3><p>​	相较于map，SortedMap为他的所有元素提供了一个整体上的排序。元素根据他们的自然顺序进行排序，或者通过在排序集创建时提供的比较器进行排序。集合的迭代器将按照升序元素的顺序遍历集合。</p>
<p>​	所有插入有序集合的元素必须实现Comparable接口。</p>
<p>​	所有通用排序集实现类都应提供四个“标准”构造函数：</p>
<ol>
<li>void（无参数）构造函数，它创建一个空的排序集，该排序集根据其元素的自然顺序排序。 </li>
<li>具有 Comparator 类型的单个参数的构造函数，它创建根据指定比较器排序的空排序集。 </li>
<li>具有单个 Collection 类型参数的构造函数，它创建一个新的有序集合，其元素与其参数相同，并根据元素的自然顺序进行排序。</li>
<li>一个带有单个 SortedMap 类型参数的构造函数，它创建一个新的有序集合，它具有与输入有序集合相同的元素和相同的顺序。</li>
</ol>
<h3 id="方法-7"><a href="#方法-7" class="headerlink" title="方法"></a>方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//返回用于对该Map中的元素进行排序的比较器</span><br><span class="hljs-comment">//如果该集合使用其元素的自然排序，则返回 null</span><br>Comparator&lt;? <span class="hljs-built_in">super</span> E&gt; comparator();<br><br><span class="hljs-comment">//返回该顺序集的部分视图</span><br>SortedMap&lt;K,V&gt; <span class="hljs-title function_">subMap</span><span class="hljs-params">(E fromKey, E toKey)</span>;<br><br><span class="hljs-comment">//返回此Map中key严格小于 toKey 的部分的视图</span><br>SortedMap&lt;K,V&gt; <span class="hljs-title function_">headSet</span><span class="hljs-params">(K toKey)</span>;<br><br><span class="hljs-comment">//返回此Map中key严格大于等于 toKey 的部分的视图</span><br>SortedMap&lt;K,V&gt; <span class="hljs-title function_">tailMap</span><span class="hljs-params">(K fromElemenfromKeyt)</span>;<br><br><span class="hljs-comment">//返回当前集合中的最小key</span><br>K <span class="hljs-title function_">firstKey</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回当前集合中的最大key</span><br>K <span class="hljs-title function_">lastKey</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的键的 Set 视图</span><br><span class="hljs-comment">//它不支持 add 或 addAll 操作</span><br>Set&lt;K&gt; <span class="hljs-title function_">keySet</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的值的集合视图</span><br><span class="hljs-comment">//它不支持 add 或 addAll 操作。</span><br>Collection&lt;V&gt; <span class="hljs-title function_">values</span><span class="hljs-params">()</span>;<br><br><span class="hljs-comment">//返回此映射中包含的映射的集合视图</span><br>Set&lt;Map.Entry&lt;K, V&gt;&gt; entrySet();<br></code></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>Java-集合源码阅读-Set篇</title>
    <url>/2023/03/10/Java/Java-%E9%9B%86%E5%90%88%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-Set%E7%AF%87/</url>
    <content><![CDATA[<p><img src="https://s3.bmp.ovh/imgs/2023/03/09/02507300fff3dde9.png"></p>
<h2 id="HashSet类（非线程安全）"><a href="#HashSet类（非线程安全）" class="headerlink" title="HashSet类（非线程安全）"></a>HashSet类（非线程安全）</h2><p>实现了Set接口</p>
<h3 id="类描述"><a href="#类描述" class="headerlink" title="类描述"></a>类描述</h3><p>​	此类实现Set接口，由哈希表（实际上是一个HashMap实例）支持。他<strong>不保证集合的迭代顺序</strong>。此类允许空元素。假设散列函数将元素适当地分散在桶中，基本操作（添加、删除、包含和大小）有<strong>恒定的时间性能</strong>。</p>
<p>​	迭代这个集合需要的时间与 HashSet 实例的大小（元素的数量）加上支持 HashMap 实例的“容量”（桶的数量）的总和成正比。因此，如果迭代性能很重要，则不要将<strong>初始容量</strong>设置得太高（或负载因子太低）。</p>
<p>​	请注意，此实现<strong>不是同步的</strong>。 如果多个线程同时访问一个散列集，并且至少有一个线程修改了该集，则它必须在外部进行同步。使用 <code>Collections.synchronizedSet</code> 方法“包装”该集合。</p>
<h3 id="类结构"><a href="#类结构" class="headerlink" title="类结构"></a>类结构</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//HashSet的底层实现</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">transient</span> HashMap&lt;E,Object&gt; map;<br><br><span class="hljs-comment">//无参构造器</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">HashSet</span><span class="hljs-params">()</span> &#123;<br>        map = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br>    &#125;<br><br><span class="hljs-comment">//含参构造器，建立的Map大小为Max(c.size/0.75,16)。</span><br><span class="hljs-comment">//也就是说最小含参构造的的HashSet大小为16</span><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">HashSet</span><span class="hljs-params">(Collection&lt;? extends E&gt; c)</span> &#123;<br>        map = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;(Math.max((<span class="hljs-type">int</span> (c.size()/<span class="hljs-number">.75f</span>) + <span class="hljs-number">1</span>, <span class="hljs-number">16</span>));<br>        addAll(c);<br>    &#125;<br></code></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li><p>事实上，Set中的每个元素是存储在Map的key中，而map的value存储的则是一个Object对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// Dummy value to associate with an Object in the backing Map</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Object</span> <span class="hljs-variable">PRESENT</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>();<br></code></pre></td></tr></table></figure></li>
</ul>
<h2 id="LinkedHashSet类（非线程安全）"><a href="#LinkedHashSet类（非线程安全）" class="headerlink" title="LinkedHashSet类（非线程安全）"></a>LinkedHashSet类（非线程安全）</h2><p>实现了Set接口，继承HashSet类</p>
<h3 id="类描述-1"><a href="#类描述-1" class="headerlink" title="类描述"></a>类描述</h3><p>​	Set 接口的哈希表和链表实现，具有<strong>可预测的迭代顺序</strong>。此实现与 HashSet 的不同之处在于它<strong>维护一个贯穿其所有条目的双向链表</strong>。 该链表定义了<strong>迭代顺序</strong>，即<strong>元素插入集合的顺序</strong>（插入顺序）。</p>
<p>​	此类提供所有可选的 Set 操作，并允许空元素。 与 HashSet 一样，它为基本操作（添加、包含和删除）提供<strong>恒定时间性能</strong>，假设散列函数将元素适当地分散在桶中。 由于维护链接列表的额外费用，<strong>性能可能略低于 HashSet</strong>，但有一个例外：迭代 LinkedHashSet 需要的时间与<strong>集合的大小</strong>成正比，而不管其容量如何。 对 HashSet 的迭代可能更昂贵，需要的时间与其<strong>容量</strong>成正比。</p>
<p>​	此实现<strong>不是同步</strong>的。使用 <code>Collections.synchronizedSet</code> 方法“包装”该集合。</p>
<h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><ul>
<li>与HashSet的区别在于<ul>
<li>HashSet基于HashMap实现，LinkedHashSet基于哈希表和链表实现。</li>
<li>HashSet迭代顺序不可预测，LinkedHashSet可以保证集合内顺序即为插入顺序。</li>
<li>HashSet的基本操作效率略高于LinkedHashSet。</li>
<li>HashSet的迭代时间与集合容量成正比，LinkedHashSet与集合的大小成正比。</li>
</ul>
</li>
</ul>
<h2 id="TreeSet类（非线程安全）"><a href="#TreeSet类（非线程安全）" class="headerlink" title="TreeSet类（非线程安全）"></a>TreeSet类（非线程安全）</h2><p>实现NavigableSet接口，继承AbstractSet类</p>
<h3 id="类描述-2"><a href="#类描述-2" class="headerlink" title="类描述"></a>类描述</h3><p>​	底层实现为NavigableMap，近似于SortedMap集合。元素使用它们的<strong>自然顺序</strong>进行排序，或者由在集合创建时提供的 <strong>Comparator</strong> 进行排序，具体取决于使用的构造函数。</p>
<p>​	此实现为基本操作（添加、删除和包含）提供有保证的 log(n) 时间成本。</p>
<p>​	此实现<strong>不是同步的</strong>。</p>
<p>​	此类的迭代器方法返回的迭代器是<strong>快速失败</strong>的。面对并发修改，迭代器会抛出异常，而不是冒着在未来不确定的时间出现任意的、不确定的行为的风险。</p>
<h3 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h3><ul>
<li>与HashSet的区别在于<ul>
<li>HashSet基于HashMap实现，LinkedHashSet基于NavigableMap实现。</li>
<li>HashSet内的元素无序，LinkedHashSet元素使用它们的<strong>自然顺序</strong>或者Comparator进行排序</li>
</ul>
</li>
</ul>
<h2 id="总结-3"><a href="#总结-3" class="headerlink" title="总结"></a>总结</h2><ul>
<li>Hashset<ul>
<li>基于HashMap实现</li>
<li>非线程安全</li>
<li>基本操作（添加删除等）性能恒定</li>
<li>迭代时间与集合容量成正比</li>
</ul>
</li>
<li>LinkedHashSet<ul>
<li>基于哈希表和双向链表实现</li>
<li>非线程安全</li>
<li>基本操作（添加删除等）性能恒定，略低于HashSet</li>
<li>可以保证迭代顺序可预测，即为元素插入顺序</li>
<li>迭代时间与集合大小成正比</li>
</ul>
</li>
<li>TreeSet<ul>
<li>基于红黑树实现</li>
<li>非线程安全</li>
<li>基本操作（添加删除等）均为O(logn)</li>
<li>可以保证集合内元素有序</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>Java-集合源码阅读-List篇</title>
    <url>/2023/03/10/Java/Java-%E9%9B%86%E5%90%88%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-List%E7%AF%87/</url>
    <content><![CDATA[<p><img src="https://s3.bmp.ovh/imgs/2023/03/09/02507300fff3dde9.png"></p>
<h2 id="ArrayList（非线程安全）"><a href="#ArrayList（非线程安全）" class="headerlink" title="ArrayList（非线程安全）"></a>ArrayList（非线程安全）</h2><p>实现list接口</p>
<h3 id="类描述"><a href="#类描述" class="headerlink" title="类描述"></a>类描述</h3><p>​	List 接口的<strong>可调整大小数组</strong>实现。实现所有可选的列表操作，并允许所有元素，<strong>包括 null</strong>。 除了实现 List 接口之外，此类还提供了一些方法来操纵内部用于存储列表的数组的大小。</p>
<p>​	<strong>size、isEmpty、get、set、iterator 和 listIterator</strong> 操作在<strong>恒定时间</strong>内运行。 添加操作以摊销<strong>常数时间</strong>运行，即添加 n 个元素需要 O(n) 时间。 所有其他操作都以线性时间运行。</p>
<p>​	应用程序可以在使用 <strong>ensureCapacity 操作</strong>添加大量元素之前增加 ArrayList 实例的容量。 这可能会<strong>减少增量重新分配的数量</strong>。</p>
<p>​	此实现<strong>不是同步</strong>的。</p>
<p>​	底层实现为<strong>Object数组</strong></p>
<h3 id="类实现"><a href="#类实现" class="headerlink" title="类实现"></a>类实现</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//ArrayList默认初始化大小</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">DEFAULT_CAPACITY</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><br><span class="hljs-comment">//空的实例的共享array实例，也就是说每次新建空的arraylist都会用这个来初始化</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Object[] EMPTY_ELEMENTDATA = &#123;&#125;;<br><br><span class="hljs-comment">//空的实例的共享array实例，也就是说每次新建空的arraylist都会用这个来初始化</span><br><span class="hljs-comment">//这个与上面那个的区别是，如果构造函数里没有传参，使用的是这个，如果构造函数里传参了但是那个参数的大小是0，就用上面那个</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = &#123;&#125;;<br><br><span class="hljs-comment">//存储 ArrayList 元素的数组缓冲区。 ArrayList 的容量就是这个数组缓冲区的长度。</span><br><span class="hljs-comment">//添加第一个元素时，任何具有 elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA 的空 ArrayList 都将扩展为 DEFAULT_CAPACITY。</span><br><span class="hljs-keyword">transient</span> Object[] elementData;<br><br><span class="hljs-comment">//有多少个元素</span><br><span class="hljs-comment">//而不是容量</span><br><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> size;<br><br><br><span class="hljs-comment">/* 扩容机制 */</span><br><br><span class="hljs-comment">//add操作会先执行ensureCapacityInternal 来确保当前容量够大</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E e)</span> &#123;<br>    ensureCapacityInternal(size + <span class="hljs-number">1</span>);  <span class="hljs-comment">// Increments modCount!!</span><br>    elementData[size++] = e;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br><br><span class="hljs-comment">//ensureCapacityInternal会执行ensureExplicitCapacity ，传参为calculateCapacity(elementData, minCapacity)</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">ensureCapacityInternal</span><span class="hljs-params">(<span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    ensureExplicitCapacity(calculateCapacity(elementData, minCapacity));<br>&#125;<br><br><span class="hljs-comment">//如果当前为DEFAULTCAPACITY_EMPTY_ELEMENTDATA，就返回默认大小和需要的最小大小的最大值</span><br><span class="hljs-comment">//否则就返回最小需要大小</span><br><span class="hljs-comment">//前面已经阐述过两个控制的区别，这里的DEFAULTCAPACITY_EMPTY_ELEMENTDATA特指构造函数未进行传参的情况。</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">calculateCapacity</span><span class="hljs-params">(Object[] elementData, <span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    <span class="hljs-keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;<br>        <span class="hljs-keyword">return</span> Math.max(DEFAULT_CAPACITY, minCapacity);<br>    &#125;<br>    <span class="hljs-keyword">return</span> minCapacity;<br>&#125;<br><br><span class="hljs-comment">//如果需要的大小比当前容量小，则进行扩容</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">ensureExplicitCapacity</span><span class="hljs-params">(<span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    <span class="hljs-comment">//此列表在结构上被修改的次数</span><br>    <span class="hljs-comment">//结构修改是那些改变列表大小的修改，或者以其他方式扰乱列表，使得正在进行的迭代可能会产生不正确的结果。</span><br>    <span class="hljs-comment">//用于检测并发操作对数组的修改</span><br>    modCount++;<br>    <span class="hljs-comment">// overflow-conscious code</span><br>    <span class="hljs-keyword">if</span> (minCapacity - elementData.length &gt; <span class="hljs-number">0</span>)<br>        grow(minCapacity);<br>    &#125;<br><br><span class="hljs-comment">//扩容，新的容量为老容量+老容量右移一位的结果，约等于老容量的1.5倍</span><br><span class="hljs-comment">//如果1.5倍仍不能满足，就设置为最小需要容量</span><br><span class="hljs-comment">//如果新容量比最大数组大小还要大，就需要进行特殊操作</span><br><span class="hljs-comment">//最大数组大小为integer最大值-8</span><br><span class="hljs-comment">//用copyof创建新的数组，再把原数组的内容拷贝进新数组，实现扩容</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">grow</span><span class="hljs-params">(<span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    <span class="hljs-comment">// overflow-conscious code</span><br>    <span class="hljs-type">int</span> <span class="hljs-variable">oldCapacity</span> <span class="hljs-operator">=</span> elementData.length;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">newCapacity</span> <span class="hljs-operator">=</span> oldCapacity + (oldCapacity &gt;&gt; <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">if</span> (newCapacity - minCapacity &lt; <span class="hljs-number">0</span>)<br>        newCapacity = minCapacity;<br>    <span class="hljs-keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="hljs-number">0</span>)<br>        newCapacity = hugeCapacity(minCapacity);<br>    <span class="hljs-comment">// minCapacity is usually close to size, so this is a win:</span><br>    elementData = Arrays.copyOf(elementData, newCapacity);<br>&#125;<br><br><span class="hljs-comment">//如果需要的最小容量比最大数组容量大，就返回int最大值</span><br><span class="hljs-comment">//否则就返回数组容量最大值</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hugeCapacity</span><span class="hljs-params">(<span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    <span class="hljs-keyword">if</span> (minCapacity &lt; <span class="hljs-number">0</span>) <span class="hljs-comment">// overflow</span><br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">OutOfMemoryError</span>();<br>    <span class="hljs-keyword">return</span> (minCapacity &gt; MAX_ARRAY_SIZE) ?<br>        Integer.MAX_VALUE :<br>        MAX_ARRAY_SIZE;<br>&#125;<br><br><br><span class="hljs-comment">/*ensureCapacity*/</span><br><br><span class="hljs-comment">//增加arraylist的容量以使其能够容量minCapacity的元素</span><br><span class="hljs-comment">//在一开始就开好大小，防止arraylist再添加过程中出现多次扩容，降低效率</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">ensureCapacity</span><span class="hljs-params">(<span class="hljs-type">int</span> minCapacity)</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">minExpand</span> <span class="hljs-operator">=</span> (elementData != DEFAULTCAPACITY_EMPTY_ELEMENTDATA)<br>       <span class="hljs-comment">// any size if not default element table</span><br>       ? <span class="hljs-number">0</span><br>       <span class="hljs-comment">// larger than default for default empty table. It&#x27;s already</span><br>       <span class="hljs-comment">// supposed to be at default size.</span><br>       : DEFAULT_CAPACITY;<br><br>     <span class="hljs-keyword">if</span> (minCapacity &gt; minExpand) &#123;<br>        ensureExplicitCapacity(minCapacity);<br>     &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>ArrayList的底层实现为Object数组</li>
<li>如果调用无参构造，数组初始化为<code>DEFAULTCAPACITY_EMPTY_ELEMENTDATA</code>，如果调用含参构造，参数为0，数组初始化为<code>EMPTY_ELEMENTDATA</code></li>
<li>ArrayList的<code>ensureCapacity</code>方法能够为数组在一开始就分配好足够的空间，避免数组进行多次扩容</li>
<li>ArrayList的默认初始化大小为10</li>
<li>ArrayList的<strong>扩容机制</strong>：当使用add操作时会调用<strong>ensureCapacityInternal</strong>方法，传入参数为size+1(即为需要的最小大小)</li>
<li>ensureCapacityInternal会调用<strong>ensureExplicitCapacity</strong>，ensureExplicitCapacity的参数为calculateCapacity</li>
<li><strong>calculateCapacity</strong>会最后计算一次需要扩容的大小，如果当前为<code>DEFAULTCAPACITY_EMPTY_ELEMENTDATA</code>,就扩容为初始化大小（10）</li>
<li><strong>ensureExplicitCapacity</strong>中会比较当前容量是否够用，如果不够就进行<strong>grow</strong></li>
<li>grow中会把新大小设为原大小的1.5倍，如果还不够就设为最小需要的大小，如果新大小超过数组最大容量（int最大值-8）就调用<strong>hugeCapacity</strong></li>
<li>hugeCapacity会判断如果最小需要大小大于数组最大大小，则设为int最大值，否则设为数组最大大小</li>
<li>扩容完成</li>
</ul>
<h2 id="LinkedList"><a href="#LinkedList" class="headerlink" title="LinkedList"></a>LinkedList</h2><p>实现list和deque接口</p>
<h3 id="类描述-1"><a href="#类描述-1" class="headerlink" title="类描述"></a>类描述</h3><p>​	List 和 Deque 接口的<strong>双向链表实现</strong>。</p>
<p>​	所有操作的执行都符合双向链表的预期。 索引到列表中的操作将<strong>从开头或结尾遍历列表</strong>，以更接近指定索引的为准。</p>
<p>​	此实现<strong>不是同步</strong>的。</p>
<h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><ul>
<li><strong>对于随机index访问的get和set方法</strong>，ArrayList的速度要优于LinkedList。因为ArrayList直接通过数组下标直接找到元素；LinkedList要移动指针遍历每个元素直到找到为止。</li>
<li><strong>新增和删除元素</strong>，LinkedList的速度要优于ArrayList。因为ArrayList在新增和删除元素时，可能扩容和复制数组；LinkedList实例化对象需要时间外，只需要修改指针即可。</li>
</ul>
<h2 id="Vector（线程安全的）"><a href="#Vector（线程安全的）" class="headerlink" title="Vector（线程安全的）"></a>Vector（线程安全的）</h2><p>实现list接口</p>
<h3 id="类描述-2"><a href="#类描述-2" class="headerlink" title="类描述"></a>类描述</h3><p>​	底层实现为<strong>Object数组</strong></p>
<p>​	是<strong>线程安全的</strong></p>
<p>​	操作效率比ArrayList低，官方文档建议如果不需要线程安全的实现，建议使用 ArrayList 代替 Vector。</p>
<p>​	扩容时新大小为原大小的两倍</p>
<h2 id="Stack（线程安全的）"><a href="#Stack（线程安全的）" class="headerlink" title="Stack（线程安全的）"></a>Stack（线程安全的）</h2><p>基于Vector类实现</p>
<p>​	</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>Java-集合源码阅读-Queue篇</title>
    <url>/2023/03/10/Java/Java-%E9%9B%86%E5%90%88%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-Queue%E7%AF%87/</url>
    <content><![CDATA[<h2 id="PriorityQueue（线程不安全）"><a href="#PriorityQueue（线程不安全）" class="headerlink" title="PriorityQueue（线程不安全）"></a>PriorityQueue（线程不安全）</h2><h3 id="类描述"><a href="#类描述" class="headerlink" title="类描述"></a>类描述</h3><p>​	基于优先级堆的无界优先级队列。底层实现为<strong>Object数组</strong>，通过设定Comparator来规定队列内的排序规则。</p>
<p>​	此实现为入队和出队方法（offer、poll、remove() 和 add）提供了 <strong>O(log(n)) 时间</strong>； remove(Object) 和 contains(Object) 方法的<strong>线性时间</strong>； 检索方法（peek、element 和 size）的<strong>固定时间</strong>。</p>
<p>​	<strong>线程不安全</strong>。</p>
<h2 id="ArrayDeque（非线程安全）"><a href="#ArrayDeque（非线程安全）" class="headerlink" title="ArrayDeque（非线程安全）"></a>ArrayDeque（非线程安全）</h2><h3 id="类描述-1"><a href="#类描述-1" class="headerlink" title="类描述"></a>类描述</h3><p>​	Deque 接口的可调整大小数组实现。数组双端队列没有容量限制； 它们会根据需要增长以支持使用。 它们<strong>不是线程安全</strong>的。</p>
<p>​	禁止空元素。 此类用作堆栈时可能比 Stack 快，用作队列时比 LinkedList 快。</p>
<p>​	大多数 ArrayDeque 操作以分摊的<strong>常数时间</strong>运行。</p>
<p>​	底层为Object数组</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>Java-集合源码阅读-Map篇</title>
    <url>/2023/03/10/Java/Java-%E9%9B%86%E5%90%88%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB-Map%E7%AF%87/</url>
    <content><![CDATA[<p><img src="https://s3.bmp.ovh/imgs/2023/03/09/02507300fff3dde9.png"></p>
<h2 id="HashMap类（非线程安全）"><a href="#HashMap类（非线程安全）" class="headerlink" title="HashMap类（非线程安全）"></a>HashMap类（非线程安全）</h2><p>实现Map接口</p>
<h3 id="类描述"><a href="#类描述" class="headerlink" title="类描述"></a>类描述</h3><p>​	Map 接口的基于<strong>哈希表</strong>的实现。<strong>允许空值和空键</strong>。 （HashMap 类大致等同于 Hashtable，只是它是非同步的并且允许空值。）此类<strong>不保证映射的顺序</strong>。</p>
<p>​	此实现为基本操作（get 和 put）提供恒定时间性能，假设散列函数将元素适当地分散在桶中。 迭代集合视图需要的时间与 HashMap 实例的“容量”（桶的数量）加上它的大小（键值映射的数量）成正比。 因此，如果迭代性能很重要，则不要将初始容量设置得太高（或负载因子太低）。</p>
<p>​	HashMap 的实例有两个影响其性能的参数：<strong>初始容量</strong>和负载因子。 容量是哈希表中桶的数量，初始容量只是创建哈希表时的容量。 负载因子是哈希表在其容量自动增加之前<strong>允许达到多满的度量</strong>。</p>
<p>​	当哈希表的条目数<strong>超过负载因子与当前容量的乘积</strong>时，哈希表将被重新哈希（即重建内部数据结构），使哈希表的桶数<strong>大约增加一倍</strong>。</p>
<p>​	作为一般规则，默认加载因子 (0.75) 提供了时间和空间成本之间的良好折衷。 较高的值会减少空间开销，但会增加查找成本。</p>
<p>​	此实现不是同步的。</p>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//HashMap默认的初始化容量为16</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">DEFAULT_INITIAL_CAPACITY</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">4</span>; <span class="hljs-comment">// aka 16</span><br><br><span class="hljs-comment">//HashMap最大的容量为2^30</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">MAXIMUM_CAPACITY</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">30</span>;<br><br><span class="hljs-comment">//默认的加载因子</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">float</span> <span class="hljs-variable">DEFAULT_LOAD_FACTOR</span> <span class="hljs-operator">=</span> <span class="hljs-number">0.75f</span>;<br><br><span class="hljs-comment">//当链表中的节点大于这个数的时候会把链表转换为红黑树</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-variable">TREEIFY_THRESHOLD</span> <span class="hljs-operator">=</span> <span class="hljs-number">8</span>;<br><br><span class="hljs-comment">//存放节点的表，在第一次使用时初始化，并根据需要调整大小。 分配时，长度始终是 2 的幂。</span><br><span class="hljs-keyword">transient</span> Node&lt;K,V&gt;[] table;<br><br> <span class="hljs-comment">// 存放具体元素的集</span><br><span class="hljs-keyword">transient</span> Set&lt;Map.Entry&lt;K,V&gt;&gt; entrySet;<br><br><span class="hljs-comment">// 存放元素的个数，注意这个不等于数组的长度。</span><br><span class="hljs-keyword">transient</span> <span class="hljs-type">int</span> size;<br><br><span class="hljs-comment">// 每次扩容和更改map结构的计数器</span><br><span class="hljs-keyword">transient</span> <span class="hljs-type">int</span> modCount;<br><br><span class="hljs-comment">// 临界值(容量*填充因子) 当实际大小超过临界值时，会进行扩容</span><br><span class="hljs-type">int</span> threshold;<br><br><span class="hljs-comment">// 加载因子</span><br><span class="hljs-keyword">final</span> <span class="hljs-type">float</span> loadFactor;<br><br><span class="hljs-comment">/* 节点 */</span><br><span class="hljs-comment">//每个节点存key，value，他的哈希值以及下一个节点</span><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Node</span>&lt;K,V&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Map</span>.Entry&lt;K,V&gt; &#123;<br>        <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> hash;<br>        <span class="hljs-keyword">final</span> K key;<br>        V value;<br>        Node&lt;K,V&gt; next;<br><br>        Node(<span class="hljs-type">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;<br>            <span class="hljs-built_in">this</span>.hash = hash;<br>            <span class="hljs-built_in">this</span>.key = key;<br>            <span class="hljs-built_in">this</span>.value = value;<br>            <span class="hljs-built_in">this</span>.next = next;<br>        &#125;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> K <span class="hljs-title function_">getKey</span><span class="hljs-params">()</span>        &#123; <span class="hljs-keyword">return</span> key; &#125;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> V <span class="hljs-title function_">getValue</span><span class="hljs-params">()</span>      &#123; <span class="hljs-keyword">return</span> value; &#125;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span> &#123; <span class="hljs-keyword">return</span> key + <span class="hljs-string">&quot;=&quot;</span> + value; &#125;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hashCode</span><span class="hljs-params">()</span> &#123;<br>            <span class="hljs-keyword">return</span> Objects.hashCode(key) ^ Objects.hashCode(value);<br>        &#125;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> V <span class="hljs-title function_">setValue</span><span class="hljs-params">(V newValue)</span> &#123;<br>            <span class="hljs-type">V</span> <span class="hljs-variable">oldValue</span> <span class="hljs-operator">=</span> value;<br>            value = newValue;<br>            <span class="hljs-keyword">return</span> oldValue;<br>        &#125;<br><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object o)</span> &#123;<br>            <span class="hljs-keyword">if</span> (o == <span class="hljs-built_in">this</span>)<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>            <span class="hljs-keyword">if</span> (o <span class="hljs-keyword">instanceof</span> Map.Entry) &#123;<br>                Map.Entry&lt;?,?&gt; e = (Map.Entry&lt;?,?&gt;)o;<br>                <span class="hljs-keyword">if</span> (Objects.equals(key, e.getKey()) &amp;&amp;<br>                    Objects.equals(value, e.getValue()))<br>                    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>            &#125;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>    &#125;<br><br><span class="hljs-comment">//HashMap中的Hash函数，具体操作是先获取key的hashCode，再把hashCode和无符号右移16位（也就是高位）进行异或计算，确保高位参与计算</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hash</span><span class="hljs-params">(Object key)</span> &#123;<br>    <span class="hljs-type">int</span> h;<br>    <span class="hljs-keyword">return</span> (key == <span class="hljs-literal">null</span>) ? <span class="hljs-number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="hljs-number">16</span>);<br>&#125;<br><br><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">putMapEntries</span><span class="hljs-params">(Map&lt;? extends K, ? extends V&gt; m, <span class="hljs-type">boolean</span> evict)</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> m.size();<br>    <span class="hljs-keyword">if</span> (s &gt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-comment">// 判断table是否已经初始化</span><br>        <span class="hljs-keyword">if</span> (table == <span class="hljs-literal">null</span>) &#123; <span class="hljs-comment">// pre-size</span><br>            <span class="hljs-comment">// 未初始化，s为m的实际元素个数</span><br>            <span class="hljs-type">float</span> <span class="hljs-variable">ft</span> <span class="hljs-operator">=</span> ((<span class="hljs-type">float</span>)s / loadFactor) + <span class="hljs-number">1.0F</span>;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> ((ft &lt; (<span class="hljs-type">float</span>)MAXIMUM_CAPACITY) ?<br>                    (<span class="hljs-type">int</span>)ft : MAXIMUM_CAPACITY);<br>            <span class="hljs-comment">// 计算得到的t大于阈值，则初始化阈值</span><br>            <span class="hljs-keyword">if</span> (t &gt; threshold)<br>                threshold = tableSizeFor(t);<br>        &#125;<br>        <span class="hljs-comment">// 已初始化，并且m元素个数大于阈值，进行扩容处理</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (s &gt; threshold)<br>            resize();<br>        <span class="hljs-comment">// 将m中的所有元素添加至HashMap中</span><br>        <span class="hljs-keyword">for</span> (Map.Entry&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">K</span>, ? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">V</span>&gt; e : m.entrySet()) &#123;<br>            <span class="hljs-type">K</span> <span class="hljs-variable">key</span> <span class="hljs-operator">=</span> e.getKey();<br>            <span class="hljs-type">V</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> e.getValue();<br>            putVal(hash(key), key, value, <span class="hljs-literal">false</span>, evict);<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//将指定值与此映射中的指定键相关联。 </span><br><span class="hljs-comment">//如果映射之前包含键的映射，则旧值将被替换。</span><br><span class="hljs-keyword">public</span> V <span class="hljs-title function_">put</span><span class="hljs-params">(K key, V value)</span> &#123;<br>    <span class="hljs-keyword">return</span> putVal(hash(key), key, value, <span class="hljs-literal">false</span>, <span class="hljs-literal">true</span>);<br>&#125;<br><br><span class="hljs-keyword">final</span> V <span class="hljs-title function_">putVal</span><span class="hljs-params">(<span class="hljs-type">int</span> hash, K key, V value, <span class="hljs-type">boolean</span> onlyIfAbsent,</span><br><span class="hljs-params">                   <span class="hljs-type">boolean</span> evict)</span> &#123;<br>    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="hljs-type">int</span> n, i;<br>    <span class="hljs-comment">// table未初始化或者长度为0，进行扩容</span><br>    <span class="hljs-keyword">if</span> ((tab = table) == <span class="hljs-literal">null</span> || (n = tab.length) == <span class="hljs-number">0</span>)<br>        n = (tab = resize()).length;<br>    <span class="hljs-comment">// (n - 1) &amp; hash 确定元素存放在哪个桶中，桶为空，新生成结点放入桶中(此时，这个结点是放在数组中)</span><br>    <span class="hljs-keyword">if</span> ((p = tab[i = (n - <span class="hljs-number">1</span>) &amp; hash]) == <span class="hljs-literal">null</span>)<br>        tab[i] = newNode(hash, key, value, <span class="hljs-literal">null</span>);<br>    <span class="hljs-comment">// 桶中已经存在元素（处理hash冲突）</span><br>    <span class="hljs-keyword">else</span> &#123;<br>        Node&lt;K,V&gt; e; K k;<br>        <span class="hljs-comment">// 判断table[i]中的元素是否与插入的key一样，若相同那就直接使用插入的值p替换掉旧的值e。</span><br>        <span class="hljs-keyword">if</span> (p.hash == hash &amp;&amp;<br>            ((k = p.key) == key || (key != <span class="hljs-literal">null</span> &amp;&amp; key.equals(k))))<br>                e = p;<br>        <span class="hljs-comment">// 判断插入的是否是红黑树节点</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (p <span class="hljs-keyword">instanceof</span> TreeNode)<br>            <span class="hljs-comment">// 放入树中</span><br>            e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="hljs-built_in">this</span>, tab, hash, key, value);<br>        <span class="hljs-comment">// 不是红黑树节点则说明为链表结点</span><br>        <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 在链表最末插入结点</span><br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">binCount</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; ; ++binCount) &#123;<br>                <span class="hljs-comment">// 到达链表的尾部</span><br>                <span class="hljs-keyword">if</span> ((e = p.next) == <span class="hljs-literal">null</span>) &#123;<br>                    <span class="hljs-comment">// 在尾部插入新结点</span><br>                    p.next = newNode(hash, key, value, <span class="hljs-literal">null</span>);<br>                    <span class="hljs-comment">// 结点数量达到阈值(默认为 8 )，执行 treeifyBin 方法</span><br>                    <span class="hljs-comment">// 这个方法会根据 HashMap 数组来决定是否转换为红黑树。</span><br>                    <span class="hljs-comment">// 只有当数组长度大于或者等于 64 的情况下，才会执行转换红黑树操作，以减少搜索时间。否则，就是只是对数组扩容。</span><br>                    <span class="hljs-keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="hljs-number">1</span>) <span class="hljs-comment">// -1 for 1st</span><br>                        treeifyBin(tab, hash);<br>                    <span class="hljs-comment">// 跳出循环</span><br>                    <span class="hljs-keyword">break</span>;<br>                &#125;<br>                <span class="hljs-comment">// 判断链表中结点的key值与插入的元素的key值是否相等</span><br>                <span class="hljs-keyword">if</span> (e.hash == hash &amp;&amp;<br>                    ((k = e.key) == key || (key != <span class="hljs-literal">null</span> &amp;&amp; key.equals(k))))<br>                    <span class="hljs-comment">// 相等，跳出循环</span><br>                    <span class="hljs-keyword">break</span>;<br>                <span class="hljs-comment">// 用于遍历桶中的链表，与前面的e = p.next组合，可以遍历链表</span><br>                p = e;<br>            &#125;<br>        &#125;<br>        <span class="hljs-comment">// 表示在桶中找到key值、hash值与插入元素相等的结点</span><br>        <span class="hljs-keyword">if</span> (e != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-comment">// 记录e的value</span><br>            <span class="hljs-type">V</span> <span class="hljs-variable">oldValue</span> <span class="hljs-operator">=</span> e.value;<br>            <span class="hljs-comment">// onlyIfAbsent为false或者旧值为null</span><br>            <span class="hljs-keyword">if</span> (!onlyIfAbsent || oldValue == <span class="hljs-literal">null</span>)<br>                <span class="hljs-comment">//用新值替换旧值</span><br>                e.value = value;<br>            <span class="hljs-comment">// 访问后回调</span><br>            afterNodeAccess(e);<br>            <span class="hljs-comment">// 返回旧值</span><br>            <span class="hljs-keyword">return</span> oldValue;<br>        &#125;<br>    &#125;<br>    <span class="hljs-comment">// 结构性修改</span><br>    ++modCount;<br>    <span class="hljs-comment">// 实际大小大于阈值则扩容</span><br>    <span class="hljs-keyword">if</span> (++size &gt; threshold)<br>        resize();<br>    <span class="hljs-comment">// 插入后回调</span><br>    afterNodeInsertion(evict);<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>&#125;<br><br><br><span class="hljs-keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;<br>    Node&lt;K,V&gt;[] oldTab = table;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">oldCap</span> <span class="hljs-operator">=</span> (oldTab == <span class="hljs-literal">null</span>) ? <span class="hljs-number">0</span> : oldTab.length;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">oldThr</span> <span class="hljs-operator">=</span> threshold;<br>    <span class="hljs-type">int</span> newCap, newThr = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">if</span> (oldCap &gt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-comment">// 超过最大值就不再扩充了，就只好随你碰撞去吧</span><br>        <span class="hljs-keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;<br>            threshold = Integer.MAX_VALUE;<br>            <span class="hljs-keyword">return</span> oldTab;<br>        &#125;<br>        <span class="hljs-comment">// 没超过最大值，就扩充为原来的2倍</span><br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="hljs-number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp; oldCap &gt;= DEFAULT_INITIAL_CAPACITY)<br>            newThr = oldThr &lt;&lt; <span class="hljs-number">1</span>; <span class="hljs-comment">// double threshold</span><br>    &#125;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (oldThr &gt; <span class="hljs-number">0</span>) <span class="hljs-comment">// initial capacity was placed in threshold</span><br>        newCap = oldThr;<br>    <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// signifies using defaults</span><br>        newCap = DEFAULT_INITIAL_CAPACITY;<br>        newThr = (<span class="hljs-type">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);<br>    &#125;<br>    <span class="hljs-comment">// 计算新的resize上限</span><br>    <span class="hljs-keyword">if</span> (newThr == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-type">float</span> <span class="hljs-variable">ft</span> <span class="hljs-operator">=</span> (<span class="hljs-type">float</span>)newCap * loadFactor;<br>        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="hljs-type">float</span>)MAXIMUM_CAPACITY ? (<span class="hljs-type">int</span>)ft : Integer.MAX_VALUE);<br>    &#125;<br>    threshold = newThr;<br>    <span class="hljs-meta">@SuppressWarnings(&#123;&quot;rawtypes&quot;,&quot;unchecked&quot;&#125;)</span><br>        Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="hljs-keyword">new</span> <span class="hljs-title class_">Node</span>[newCap];<br>    table = newTab;<br>    <span class="hljs-keyword">if</span> (oldTab != <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-comment">// 把每个bucket都移动到新的buckets中</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; j &lt; oldCap; ++j) &#123;<br>            Node&lt;K,V&gt; e;<br>            <span class="hljs-keyword">if</span> ((e = oldTab[j]) != <span class="hljs-literal">null</span>) &#123;<br>                oldTab[j] = <span class="hljs-literal">null</span>;<br>                <span class="hljs-keyword">if</span> (e.next == <span class="hljs-literal">null</span>)<br>                    newTab[e.hash &amp; (newCap - <span class="hljs-number">1</span>)] = e;<br>                <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (e <span class="hljs-keyword">instanceof</span> TreeNode)<br>                    ((TreeNode&lt;K,V&gt;)e).split(<span class="hljs-built_in">this</span>, newTab, j, oldCap);<br>                <span class="hljs-keyword">else</span> &#123;<br>                    Node&lt;K,V&gt; loHead = <span class="hljs-literal">null</span>, loTail = <span class="hljs-literal">null</span>;<br>                    Node&lt;K,V&gt; hiHead = <span class="hljs-literal">null</span>, hiTail = <span class="hljs-literal">null</span>;<br>                    Node&lt;K,V&gt; next;<br>                    <span class="hljs-keyword">do</span> &#123;<br>                        next = e.next;<br>                        <span class="hljs-comment">// 原索引</span><br>                        <span class="hljs-keyword">if</span> ((e.hash &amp; oldCap) == <span class="hljs-number">0</span>) &#123;<br>                            <span class="hljs-keyword">if</span> (loTail == <span class="hljs-literal">null</span>)<br>                                loHead = e;<br>                            <span class="hljs-keyword">else</span><br>                                loTail.next = e;<br>                            loTail = e;<br>                        &#125;<br>                        <span class="hljs-comment">// 原索引+oldCap</span><br>                        <span class="hljs-keyword">else</span> &#123;<br>                            <span class="hljs-keyword">if</span> (hiTail == <span class="hljs-literal">null</span>)<br>                                hiHead = e;<br>                            <span class="hljs-keyword">else</span><br>                                hiTail.next = e;<br>                            hiTail = e;<br>                        &#125;<br>                    &#125; <span class="hljs-keyword">while</span> ((e = next) != <span class="hljs-literal">null</span>);<br>                    <span class="hljs-comment">// 原索引放到bucket里</span><br>                    <span class="hljs-keyword">if</span> (loTail != <span class="hljs-literal">null</span>) &#123;<br>                        loTail.next = <span class="hljs-literal">null</span>;<br>                        newTab[j] = loHead;<br>                    &#125;<br>                    <span class="hljs-comment">// 原索引+oldCap放到bucket里</span><br>                    <span class="hljs-keyword">if</span> (hiTail != <span class="hljs-literal">null</span>) &#123;<br>                        hiTail.next = <span class="hljs-literal">null</span>;<br>                        newTab[j + oldCap] = hiHead;<br>                    &#125;<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> newTab;<br>&#125;<br><br><span class="hljs-comment">//这个方法的作用是把传进的数转换为不小于它的2的次幂</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">tableSizeFor</span><span class="hljs-params">(<span class="hljs-type">int</span> cap)</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">n</span> <span class="hljs-operator">=</span> cap - <span class="hljs-number">1</span>;<br>    n |= n &gt;&gt;&gt; <span class="hljs-number">1</span>;<br>    n |= n &gt;&gt;&gt; <span class="hljs-number">2</span>;<br>    n |= n &gt;&gt;&gt; <span class="hljs-number">4</span>;<br>    n |= n &gt;&gt;&gt; <span class="hljs-number">8</span>;<br>    n |= n &gt;&gt;&gt; <span class="hljs-number">16</span>;<br>    <span class="hljs-keyword">return</span> (n &lt; <span class="hljs-number">0</span>) ? <span class="hljs-number">1</span> : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + <span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>每次容量大于临界容量时会进行扩容，扩容为原先的2倍大小，然后把原先的node放到扩容后的table中。</li>
<li>为什么变成2倍？因为每个节点放到的table索引的计算方式为(length-1)&amp;hash，所以如果变成2倍，我们只需要考虑hash最高位是否为1，如果为1，新的index就为原index+原table长度，如果为0则不变。可以大大减少resize时重新插入的开销。</li>
<li>put方法流程<ul>
<li>如果table未进行初始化，则进行初始化。</li>
<li>通过key计算hash，再通过hash计算index</li>
<li>查看index处是否有元素，若无，直接插入。若有则解决冲突</li>
<li>若为链表节点，直接插入最后，若链表中节点超过阈值，转换为红黑树</li>
<li>若为红黑树节点，添加进红黑树</li>
<li>查看大小是否超过临界容量，若超过则进行扩容</li>
</ul>
</li>
</ul>
<h2 id="LinkedHashMap（非线程安全）"><a href="#LinkedHashMap（非线程安全）" class="headerlink" title="LinkedHashMap（非线程安全）"></a>LinkedHashMap（非线程安全）</h2><h3 id="类描述-1"><a href="#类描述-1" class="headerlink" title="类描述"></a>类描述</h3><p>​	Map 接口的哈希表和链表实现，具有<strong>可预测的迭代顺序</strong>。 此实现与 HashMap 的不同之处在于它维护一个贯穿其所有条目的<strong>双向链表。</strong> 该链表定义了迭代顺序，通常是<strong>将键插入映射的顺序</strong>（插入顺序）。 请注意，如果将键重新插入到映射中，插入顺序不会受到影响。</p>
<p>​	此实现<strong>不是同步的</strong></p>
<h2 id="HashTree（线程安全）"><a href="#HashTree（线程安全）" class="headerlink" title="HashTree（线程安全）"></a>HashTree（线程安全）</h2><h3 id="类描述-2"><a href="#类描述-2" class="headerlink" title="类描述"></a>类描述</h3><p>​	此类实现了一个哈希表，它将键映射到值。 <strong>任何非空对象</strong>都可以用作键或值。HashTable直接使用对象的hashCode。而HashMap重新计算hash值。</p>
<p>​	此实现<strong>是同步的</strong></p>
<h2 id="TreeMap（非线程安全）"><a href="#TreeMap（非线程安全）" class="headerlink" title="TreeMap（非线程安全）"></a>TreeMap（非线程安全）</h2><p>​	基于<strong>红黑树</strong>的 NavigableMap 实现。 映射根据其键的自然顺序进行排序，或者根据使用的构造函数在映射创建时<strong>提供的比较器</strong>进行排序。<br>​	此实现为 containsKey、get、put 和 remove 操作提供了有保证的 <strong>log(n)</strong> 时间成本。</p>
<p>​	此实现<strong>不是同步的</strong>。</p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>tableSizeFor的作用！<a href="https://blog.csdn.net/ywb201314/article/details/120022308">https://blog.csdn.net/ywb201314/article/details/120022308</a></p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>算法-排序算法</title>
    <url>/2023/03/11/%E7%AE%97%E6%B3%95/%E7%AE%97%E6%B3%95-%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>leetcode题目索引:<a href="https://leetcode.cn/problems/kth-largest-element-in-an-array/">215. 数组中的第K个最大元素</a></p>
<p><strong><u><em>事实上我的算法基础是真的差…如果大一的时候好好学数据结构该多好</em></u></strong></p>
<h2 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h2><p>​	腾讯经常会问到的排序算法，典型场景就是几百万的数据量，要求获取一个第k大的数据</p>
<p>​	堆作为一个完全二叉树，拥有二叉树的特殊性质</p>
<p>​	</p>
<table>
<thead>
<tr>
<th align="center">节点下标</th>
<th align="center">父亲节点</th>
<th align="center">左子节点</th>
<th align="center">右子节点</th>
</tr>
</thead>
<tbody><tr>
<td align="center">i</td>
<td align="center">(i-1)&#x2F;2</td>
<td align="center">2*i+1</td>
<td align="center">2*i+2</td>
</tr>
</tbody></table>
<p>​	我们知道对于一个数组而言，知道一个数的下标就很容易计算出他的父亲和儿子节点的下标，因此在数组上建立堆的操作会较为简便。</p>
<p>​	以最大堆为例，最大堆要求他的任意一个节点的大小大于其所有儿子节点。那么建立最大堆的操作就是一个递归的操作。</p>
<p>​	递归的操作内容为：比较当前节点和所有儿子节点，选择一个最大的节点（比如为left），然后将当前节点与left节点交换，再递归处理交换后的left节点。如果当前节点是最大的，就不进行操作。</p>
<p>​	这里需要理解为什么要递归操作交换后的节点，因为我们不能保证交换后的节点是其子树中的最大节点，因此需要对该节点再次进行一次操作。</p>
<p>​	那么递归的退出条件是什么？当处理节点下标&gt;&#x3D;数组长度时，就要返回了。<br>​	由于这个操作很像把最上面的节点下沉到下面，因此我们把这个操作叫做<strong>下沉</strong></p>
<p>​	那么将任务拆分，只需要对完全二叉树中的每个父亲节点都进行一次递归的<strong>下沉</strong>操作，就能够获得一个最大堆了。</p>
<p>​	那么最后一个父亲节点的序号是多少？我们知道最后一个节点的下标为nums.length-1，那么他的父亲节点作为最后一个父亲节点，他的下标就为(nums.length-1-1)&#x2F;2，从这个下标开始一直到0的都为父亲节点，对这些节点进行下沉操作就行了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">heapify</span><span class="hljs-params">(<span class="hljs-type">int</span>[] num,<span class="hljs-type">int</span> root,<span class="hljs-type">int</span> size)</span>&#123;<br>    <span class="hljs-keyword">if</span>(root &gt;= size)<span class="hljs-keyword">return</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">l</span> <span class="hljs-operator">=</span> <span class="hljs-number">2</span>*root + <span class="hljs-number">1</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> <span class="hljs-number">2</span>*right + <span class="hljs-number">2</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">max</span> <span class="hljs-operator">=</span> root;<br>    <span class="hljs-keyword">if</span>(l &lt; size)&#123;<br>        <span class="hljs-keyword">if</span>(num[l]&gt;num[max])max = l;<br>    &#125;<br>    <span class="hljs-keyword">if</span>(r &lt; size)&#123;<br>        <span class="hljs-keyword">if</span>(num[r]&gt;num[max])max = r;<br>    &#125;<br>    <span class="hljs-keyword">if</span>(max != root)&#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span> num[max];<br>        num[max] = num[root];<br>        num[root] = tmp;<br>        heapify(num,max,size);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">buildMaxHeap</span><span class="hljs-params">(<span class="hljs-type">int</span>[] num)</span>&#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">size</span> <span class="hljs-operator">=</span> num.length;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> (size-<span class="hljs-number">1</span>-<span class="hljs-number">1</span>)/<span class="hljs-number">2</span>;i&gt;=<span class="hljs-number">0</span>;i--)<br>    	heapify(num,i,size);<br>&#125;<br></code></pre></td></tr></table></figure>

<p>​	再讲讲力扣上的这道题怎么用堆排序解，建立完最大堆后，每次把堆顶和最后一个元素交换，把数组大小-1，再对堆顶进行一次下沉操作。进行k-1次操作后的堆顶就是第k大的数值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">size</span> <span class="hljs-operator">=</span> nums.length;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span> ;i &lt; k-<span class="hljs-number">1</span> ;i ++)&#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span> nums[<span class="hljs-number">0</span>];<br>    nums[<span class="hljs-number">0</span>] = nums[--size];<br>    nums[size] = tmp;<br>    heapify(nums,<span class="hljs-number">0</span>,size);<br>&#125;<br><span class="hljs-keyword">return</span> nums[<span class="hljs-number">0</span>];<br></code></pre></td></tr></table></figure>

<h2 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h2><p>快速排序就是分治和递归的思想，本质上也不难，可是我大学三年都不一定能写出来，mmd</p>
<p>先上代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">fastSortInternal</span><span class="hljs-params">(<span class="hljs-type">int</span>[] nums,<span class="hljs-type">int</span> left,<span class="hljs-type">int</span> right)</span>&#123;<br>    <span class="hljs-keyword">if</span>(left &gt;= right)<span class="hljs-keyword">return</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">l</span> <span class="hljs-operator">=</span> left;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">r</span> <span class="hljs-operator">=</span> right;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">pivot</span> <span class="hljs-operator">=</span> left;<br>    <span class="hljs-keyword">while</span>(left &lt; right)&#123;<br>        <span class="hljs-keyword">while</span> (left&lt;right&amp;&amp;nums[right]&lt;=nums[pivot])right--;<br>        <span class="hljs-keyword">while</span> (left&lt;right&amp;&amp;nums[left]&gt;=nums[pivot])left++;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span> nums[left];<br>        nums[left] = nums[right];<br>        nums[right] = tmp;<br>    &#125;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">tmp</span> <span class="hljs-operator">=</span> nums[left];<br>    nums[left] = nums[pivot];<br>    nums[pivot] = tmp;<br>    fastSortInternal(nums,l,left-<span class="hljs-number">1</span>);<br>    fastSortInternal(nums,left+<span class="hljs-number">1</span>,r);<br>&#125;<br><br> <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">fastSort</span><span class="hljs-params">(<span class="hljs-type">int</span>[] nums,<span class="hljs-type">int</span> k)</span>&#123;<br>    fastSortInternal(nums,<span class="hljs-number">0</span>,nums.length-<span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">return</span> nums[k-<span class="hljs-number">1</span>];<br>&#125;<br></code></pre></td></tr></table></figure>

<p>​	对于每一次排序，选最左边的一个数为标准值，然后使用left和right双指针，left从左往右遍历直到指向一个比标准值小的数，right从右往左遍历直到指向一个比标准值大的数。然后把这两个指针指向的数交换。重复以上操作指导left与right相遇。把标准值和left与right相遇的index一交换，一次排序就算结束了。</p>
<p>​	那么分治呢？别急，我们把left与right相遇的index称为index，index左边的数都比标准值大，右边的都比标准值小，那么只需要递归地分别对左边和右边进行排序就行了。</p>
<p>​	递归的结束？当left&gt;&#x3D;right就结束了。</p>
<p>​	快排的思想还是很简单的。</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>java-并发</title>
    <url>/2023/03/11/Java/java-%E5%B9%B6%E5%8F%91/</url>
    <content><![CDATA[<p><u><em><strong>真够吧难啊并发！！！感觉是最难的一章了，绷不住。我现在看了到时候面试官如果不问我就心态崩了！！！</strong></em></u></p>
<h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><h3 id="啥是线程？"><a href="#啥是线程？" class="headerlink" title="啥是线程？"></a>啥是线程？</h3><p>​	从操作系统的角度来说，线程是操作系统进行<strong>运算调度</strong>的最小单位。是进程中的一个执行单元，负责当前进程的执行，一个进程中可以有多个线程。操作系统对线程的管理分为KLT，ULT和混合型。这里就不再展开说明了。</p>
<p>​	从Java的角度来说，一个线程拥有自己独立的线程栈。包含java虚拟机栈，本地方法栈和PC程序计数器，在运行java程序时以线程为最小单位。</p>
<h3 id="和进程有啥区别？"><a href="#和进程有啥区别？" class="headerlink" title="和进程有啥区别？"></a>和进程有啥区别？</h3><p>​	我们知道线程有自己独立的线程栈，但是他们共用进程的方法区和堆。也就是说一个进程下的多个线程共用一个方法区和堆。进程是操作系统进行<strong>资源分配和管理</strong>的基本单位。</p>
<h3 id="线程的生命周期和状态？"><a href="#线程的生命周期和状态？" class="headerlink" title="线程的生命周期和状态？"></a>线程的生命周期和状态？</h3><ul>
<li><strong>NEW</strong>：初始状态，当一个线程被创建出来，而还未对他进行start()操作时，这个线程就处于一个一个<strong>NEW</strong>状态。</li>
<li><strong>RUNNABLE</strong>:运行状态，好，这个线程被调用了start()，他成功的开始运行了</li>
<li><strong>BLOCKED</strong>:阻塞状态，一般是线程在运行过程中无法抢到锁，而被阻塞</li>
<li><strong>WAITING</strong>:等待状态，此时线程释放掉他的锁，等待一些特定动作（如其他线程来通知他）</li>
<li><strong>TIME_WAITING</strong>:超时等待状态，和waiting很像，只不过他会在一段时间后自动开始运行</li>
<li><strong>TERMINATED</strong>:终止状态，这个线程运行完了，就进入终止状态</li>
</ul>
<p>​	总结下来其实和进程三态五态差不了太多，等待运行，运行，阻塞，等待和终止态呗！</p>
<h2 id="sleep-和wait-有啥区别"><a href="#sleep-和wait-有啥区别" class="headerlink" title="sleep()和wait()有啥区别!"></a>sleep()和wait()有啥区别!</h2><ul>
<li>sleep()是thread类的方法，而wait()是object的方法</li>
<li>sleep()的线程仍然会持有锁，而wait的线程会把自己拥有的锁释放掉</li>
<li>sleep()是暂停一个线程的执行，而wait通常用于管理线程间的交互和通信</li>
<li>sleep()的线程会在执行后自动苏醒，而wait往往要等待其他特定动作</li>
</ul>
<h2 id="锁（小时候最爱看的一集）"><a href="#锁（小时候最爱看的一集）" class="headerlink" title="锁（小时候最爱看的一集）"></a>锁（小时候最爱看的一集）</h2><h3 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h3><p>​	这个关键字比较逆天，他只能修饰<strong>单独的一个变量</strong>！他不能修饰方法或者类这些的。他的作用也很单一：保证这个类的<em>可见性</em>。具体就是当JVM看到这个关键字，他就知道这个变量是共享的且不稳定的，每次使用它都必须到主存中进行读取。</p>
<p>​	他不能保证数据的原子性。</p>
<p>​	我们知道JVM为了提高性能，常常会对指令做重排序。但有些时候对代码进行重排序会导致验证的可见性问题，这时候在变量上加上<strong>volatile</strong>关键字就可以禁止指令重排序！</p>
<h3 id="原子性和可见性"><a href="#原子性和可见性" class="headerlink" title="原子性和可见性"></a>原子性和可见性</h3><p>​	那么原子性和可见性的区别是什么？</p>
<ul>
<li>原子性：保证指令不会受到线程上下文切换的影响</li>
<li>可见性：保证指令不会受到cpu缓存的影响</li>
</ul>
<h3 id="乐观锁和悲观锁"><a href="#乐观锁和悲观锁" class="headerlink" title="乐观锁和悲观锁"></a>乐观锁和悲观锁</h3><h4 id="悲观锁"><a href="#悲观锁" class="headerlink" title="悲观锁"></a>悲观锁</h4><p>​	悲观锁的概念其实很简单，就是一旦进入代码的临界区，就要进行加锁。它假设对贡献资源的每次访问都会出现并发问题，也就是说，贡献资源每次只能被一个线程使用，用完后才能转给其他线程。</p>
<p>​	如<strong>synchronized</strong>和<strong>ReentrantLock</strong>就是悲观锁的实现。</p>
<p>​	多用于写较多的情况下，避免频繁失败和重试影响性能。</p>
<h4 id="乐观锁"><a href="#乐观锁" class="headerlink" title="乐观锁"></a>乐观锁</h4><p>​	乐观锁就是反一反，认为每次对共享资源的访问都不会出现问题，他把共享资源开放给所有线程，只需要在线程提交数据时去验证一下资源是否被其他线程修改了即可。</p>
<p>​	多用于写较少的情况下，大大提升系统的吞吐量。</p>
<h4 id="如何实现乐观锁"><a href="#如何实现乐观锁" class="headerlink" title="如何实现乐观锁"></a>如何实现乐观锁</h4><ul>
<li>版本号机制，对每个数据记录一个版本号，数据修改后再次提交需要修改版本号，如果提交时发现版本号不同，则拒绝提交</li>
<li><strong>CAS</strong></li>
</ul>
<h4 id="CAS"><a href="#CAS" class="headerlink" title="CAS"></a>CAS</h4><p>​	CAS的思想就是利用三个数据:V(要更新的变量值),E(预期值),N(要写入的新值)，具体就是比较要更新的变量值和预期值是否相同，如果相同就写入，如果不同则拒绝写入并进行自旋操作。</p>
<h4 id="CAS存在的问题"><a href="#CAS存在的问题" class="headerlink" title="CAS存在的问题"></a>CAS存在的问题</h4><ul>
<li>ABA问题：如果现在的值和预期值相同，就能保证该变量未被修改过吗？这个推论显然是不正确的，因为我们无法保证这个数据在修改后又修改回原先的值，而这种情形在CAS中显然无法检测出。具体的解决方案就是结合版本号，在比较数据值的同时比较版本号。</li>
<li>时间开销大：在拒绝修改后，CAS会进入频繁的自旋重试操作，也就是不成功就一直循环直到成功，这个操作其实要花费大量的时间和CPU资源</li>
<li>只能保证一个共享变量的原子性：只对单个共享变量有效</li>
</ul>
<h3 id="Synchronized"><a href="#Synchronized" class="headerlink" title="Synchronized"></a>Synchronized</h3><p>​	synchronized保证被他修饰的方法或者类在同一时间只能有一个线程在执行，实现了代码和数据的原子性。</p>
<p>​	synchronized的使用方式主要有三种：</p>
<ul>
<li>修饰实例方法（对当前对象实例上锁）<ul>
<li>对当前对象上锁</li>
<li>进入临界区要求获取当前对象实例的锁</li>
</ul>
</li>
<li>修饰静态方法（对当前类上锁）<ul>
<li>对当前类上锁，会作用于该类的所有对象实例</li>
<li>进入临界区要求获取当前class的锁</li>
</ul>
</li>
<li>修饰代码块（锁指定对象&#x2F;类）<ul>
<li>对括号内指定的对象或类加锁</li>
</ul>
</li>
</ul>
<h4 id="底层原理"><a href="#底层原理" class="headerlink" title="底层原理"></a>底层原理</h4><p>​	底层原理是：</p>
<ul>
<li>同步语句块实际使用的是monitorenter和monitorexit指令，enter指向同步代码块的开始位置，exit指名结束位置。实际上是获取<strong>对象监视器monitor</strong>的持有权。</li>
<li>如果当前monitor锁标识符为0，则获取并把标识符设置为1</li>
<li>否则被阻塞</li>
<li>修饰的方法通过一个标识指名该方法是一个同步方法，JVM看到这个标识后会辨别是否为同步方法并指向相应的同步调用</li>
<li><strong>不过两者的本质都是对对象监视器 monitor 的获取。</strong></li>
</ul>
<h3 id="ReentrantLock"><a href="#ReentrantLock" class="headerlink" title="ReentrantLock"></a>ReentrantLock</h3><p>​	ReentrantLock实现了Lock接口，是一个可重入且独占式的锁。和synchronized类似，只不过更加灵活强大，且增加了轮询，超时中断和公平非公平锁的高级功能。</p>
<h4 id="公平锁和非公平锁的区别"><a href="#公平锁和非公平锁的区别" class="headerlink" title="公平锁和非公平锁的区别"></a>公平锁和非公平锁的区别</h4><ul>
<li>公平锁：一个锁被释放后，锁会给一个最先申请的线程（也就是维护一个等待队列，把锁给等待队列的第一个线程），性能会差一些，因为要保证时间上的绝对顺序会导致上下文切换频繁</li>
<li>非公平锁：锁被释放后可能给随即队列中的任意一个线程，性能较好但会导致某些线程饿死</li>
</ul>
<h4 id="synchronized-和-ReentrantLock-有什么区别？"><a href="#synchronized-和-ReentrantLock-有什么区别？" class="headerlink" title="synchronized 和 ReentrantLock 有什么区别？"></a>synchronized 和 ReentrantLock 有什么区别？</h4><ul>
<li>synchronized和ReentrantLock都是可重入锁，指的是线程可以再次获取自己的内部锁。就是指一个线程获取了某个对象的锁，如果此时锁没有被释放，该线程仍然可以获取该对象的锁。如果是不可重入锁很可能会导致死锁</li>
<li>synchronized是基于JVM实现的,ReentrantLock是基于Java API实现的</li>
<li>ReentrantLock提供等待可中断的功能，一个线程可以在等待的过程中选择放弃等待而改去处理其他事情。</li>
<li>ReentrantLock可以实现公平锁和非公平锁，而synchronized只能是非公平锁</li>
</ul>
<h4 id="可中断锁和不可中断锁有什么区别？"><a href="#可中断锁和不可中断锁有什么区别？" class="headerlink" title="可中断锁和不可中断锁有什么区别？"></a>可中断锁和不可中断锁有什么区别？</h4><ul>
<li><strong>可中断锁</strong> ：获取锁的过程中可以被中断，不需要一直等到获取锁之后 才能进行其他逻辑处理。<code>ReentrantLock</code> 就属于是可中断锁。</li>
<li><strong>不可中断锁</strong> ：一旦线程申请了锁，就只能等到拿到锁以后才能进行其他的逻辑处理。 <code>synchronized</code> 就属于是不可中断锁。</li>
</ul>
<h2 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h2><p>​	我们知道，我们创建的变量是可以被任何一个线程访问并修改的。但我们可以通过ThreadLocal来创建一个当前线程专用的数据盒子。每个访问ThreadLocal变量的线程都会有这个变量的本地副本，从而避免了线程安全问题。</p>
<h3 id="底层实现"><a href="#底层实现" class="headerlink" title="底层实现"></a>底层实现</h3><p>​	ThreadLocal实际上是由ThreadLocalMap实现的。在一个thread中可以创建多个ThreadLocal变量，但ThreadLocalMap只有一个。Map存储的是键值对，其中key是ThreadLocal，值为Object对象。每次访问threadLocal时，先获取当前的thread，在获取map，通过这个map获取local对象。</p>
<p>​	因此可以把local理解为map的封装，传递了变量值</p>
<h2 id="线程池"><a href="#线程池" class="headerlink" title="线程池"></a>线程池</h2><p>​	线程池就是管理一系列线程的资源池。当有任务要处理时，直接从线程池中获取线程来处理，处理完之后线程不会被立即销毁，而是等待下一次任务。</p>
<h3 id="为什么要使用线程池？"><a href="#为什么要使用线程池？" class="headerlink" title="为什么要使用线程池？"></a>为什么要使用线程池？</h3><ul>
<li><strong>降低资源消耗</strong>。通过重复利用已经创建好的线程来降低线程创建时的资源消耗。</li>
<li><strong>提高响应速度</strong>。当任务到达时，可以不等待线程创建而直接获得一个空闲线程来执行</li>
<li><strong>提高线程的可管理性</strong>。如果将线程创建和删除的过程不进行统一管理，会导致创建的线程格式不一致，重复创建等问题。通过同一管理线程，可以节约系统资源，提高系统的可管理性。</li>
</ul>
<h3 id="线程池的参数有那些？"><a href="#线程池的参数有那些？" class="headerlink" title="线程池的参数有那些？"></a>线程池的参数有那些？</h3><p>​	分析参数前，我们需要先了解线程池的执行过程</p>
<p><img src="https://s3.bmp.ovh/imgs/2023/03/11/5571dc550fafe74a.png"></p>
<ol>
<li>当线程池内存活的线程小于核心线程数<code>corePoolsize</code>时，新提交的任务会创建一个核心线程区处理任务。对于这部分线程，他们会一直存活着，即使空闲时间超出了<code>keepAliveTime</code>也不会被释放。</li>
<li>当线程池内存活的线程已经达到核心线程数了，对于一个新添加的任务，会放到<strong>任务队列workQueue</strong>中排队等待执行。</li>
<li>当任务队列也排满了！此时新的任务又来了，线程池会创建新的线程来处理任务这部分的线程就是超过核心线程数但未达到<code>maximumPoolSize</code>的部分，这部分线程在超出最长空闲时间后会被自动释放</li>
<li>如果当前线程数以达到最大线程数，新任务过来时会采取<strong>拒绝策略</strong>进行处理。</li>
</ol>
<p>​	那么线程池参数就显而易见了</p>
<ul>
<li><strong>corepoolsize</strong>:线程池核心线程数量</li>
<li><strong>maximumPoolSize</strong>：线程池最大线程数量</li>
<li><strong>BlockingQueue</strong>:存储等待运行的任务</li>
<li><strong>keepAliveTime</strong>:不在核心线程处的线程当空闲时间超过这个就会被自动释放</li>
<li><strong>TimeUnit</strong>:时间单位</li>
<li><strong>ThreadFactory</strong>:对于每个线程，会使用工厂同一对线程进行创建</li>
<li><strong>RejectedExecutionHandler</strong>:当超出最大线程数时采取的拒绝策略</li>
</ul>
<h2 id="Future类"><a href="#Future类" class="headerlink" title="Future类"></a>Future类</h2><p>​	<code>Future</code> 类是异步思想的典型运用，主要用在一些需要<strong>执行耗时任务</strong>的场景，避免程序一直原地等待耗时任务执行完成，执行效率太低。具体来说是这样的：当我们执行某一耗时的任务时，可以将这个耗时任务交给一个子线程去异步执行，同时我们可以干点其他事情，不用傻傻等待耗时任务执行完成。等我们的事情干完后，我们再通过 <code>Future</code> 类获取到耗时任务的执行结果。这样一来，程序的执行效率就明显提高了。</p>
<p>下帖一个关于concurrentHashMap的博客</p>
<p><a href="https://blog.csdn.net/qq_42068856/article/details/126091526">https://blog.csdn.net/qq_42068856/article/details/126091526</a></p>
<p>​	</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>并发</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL-索引</title>
    <url>/2023/03/12/%E6%95%B0%E6%8D%AE%E5%BA%93/MySQL-%E7%B4%A2%E5%BC%95/</url>
    <content><![CDATA[<h2 id="索引介绍"><a href="#索引介绍" class="headerlink" title="索引介绍"></a>索引介绍</h2><p>​	是一种用于快速查询和检索数据的数据结构，本质上可以看成是一种排序好的数据结构</p>
<h2 id="索引的优缺点"><a href="#索引的优缺点" class="headerlink" title="索引的优缺点"></a>索引的优缺点</h2><ul>
<li>优点<ul>
<li>可以加快数据查询速度</li>
<li>通过构建唯一索引，可以保证数据库中的记录不存在冗余</li>
<li>为用来排序或者分组的字段添加索引，可以加快排序和分组的速度</li>
<li>加快表与表之间的连接操作</li>
</ul>
</li>
<li>缺点<ul>
<li>每次对表进行更新都要修改索引，操作繁琐</li>
<li>索引会占用大量物理空间</li>
</ul>
</li>
</ul>
<h2 id="索引的数据结构"><a href="#索引的数据结构" class="headerlink" title="索引的数据结构"></a>索引的数据结构</h2><h3 id="哈希索引"><a href="#哈希索引" class="headerlink" title="哈希索引"></a>哈希索引</h3><p>​	使用哈希表构建索引，可以快速的通过key取出相应的value。</p>
<p>​	<strong>做全键值匹配操作</strong>时效率远远大于b树索引。</p>
<p>​	但是不支持<strong>范围查找</strong>和模糊查找，同时<strong>存在碰撞率的问题</strong></p>
<h3 id="B树和B-树索引"><a href="#B树和B-树索引" class="headerlink" title="B树和B+树索引"></a>B树和B+树索引</h3><h4 id="B树和B-树的区别"><a href="#B树和B-树的区别" class="headerlink" title="B树和B+树的区别"></a>B树和B+树的区别</h4><ul>
<li>B树的所有节点既存放数据又存放键，B+树只有叶子节点存放值和键</li>
<li>B树的叶子节点是独立的，B+树的叶子节点有一条引用链指向相邻的叶子节点</li>
<li>B树的检索过程是对范围内的每个节点的关键字作二分查找，可能还没到达叶子节点就结束了，B+树的检索效率稳定，都是从根节点到叶子节点</li>
</ul>
<p>​	在B+树中，节点的key从左到右递增排列，如果某个指针左右相邻key分别为keyi和keyi+1，则该指针指向节点的所有key大于等于keyi小于等于 keyi+1。</p>
<p>​	进行查找操作时，首先在根节点进行二分查找，找到key 所在的指针，然后递归地在指针所指向的节点进行查找。直到查找到叶子节点，然后在叶子节点上进行二分查找，找出key 所对应的数据项。</p>
<h3 id="为什么采用B-而不是B树"><a href="#为什么采用B-而不是B树" class="headerlink" title="为什么采用B+而不是B树"></a>为什么采用B+而不是B树</h3><ul>
<li>B+树的扫库操作特别方便，只需要扫一遍叶子节点就行了。而B树的数据分散在各个节点中，需要进行一边中序遍历才能找到具体的数据，所以B+树更适合区间查询。而数据库中的区间查询较为频繁</li>
<li>B+树中的节点只存储key值，使得索引中可以存放更多的节点，减少更多的IO支出</li>
<li>B+树的查询效率稳定，任何关键字的查找必须走一条从根节点到叶子节点的路，每一个数据的查询效率都相当</li>
</ul>
<h2 id="索引的分类"><a href="#索引的分类" class="headerlink" title="索引的分类"></a>索引的分类</h2><ul>
<li><strong>主键索引</strong>：名为primary的唯一非空索引，不允许有空值</li>
<li><strong>唯一索引</strong>：索引列中的值唯一，但允许有空值</li>
<li><strong>组合索引</strong>：在多个字段上创建的索引，须遵循最左匹配原则</li>
<li><strong>全文索引</strong>：只能在CHAR,VARCHAR,TEXT等字段上使用全文索引</li>
<li><strong>普通索引</strong>：最基本的索引</li>
</ul>
<h2 id="聚簇索引"><a href="#聚簇索引" class="headerlink" title="聚簇索引"></a>聚簇索引</h2><p>​	InnoDB使用表的主键构建主键索引树，同时叶子节点中存放的即为整张表的记录数据。</p>
<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>查询速度非常快：定位到节点就相当于定位到了数据，少了个回表操作</li>
<li>对排序查找和范围查找优化</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>依赖于有序的数据</li>
<li>更新代价大</li>
</ul>
<h2 id="覆盖索引"><a href="#覆盖索引" class="headerlink" title="覆盖索引"></a>覆盖索引</h2><p>​	如果需要查询的字段正好是索引的字段，则可以直接根据该索引查到数据，而不需要进行回表操作。</p>
<p>​	例如查询name，而刚好根据name这个字段创建了索引，那么查到相应的节点后返回索引的key就行了，不需要再回表查询。</p>
<h2 id="最左前缀匹配原则"><a href="#最左前缀匹配原则" class="headerlink" title="最左前缀匹配原则"></a>最左前缀匹配原则</h2><p>​	使用组合索引时要注意的原则，主要内容是在使用组合索引时，MySQL会根据联合索引中的字段顺序，从左到右依次到查询条件中去匹配。如果遇到范围查询时会停止匹配。所以在使用组合索引时，可以将区分度高的字段放到最左边，可以过滤掉更多数据。</p>
<p>​	对(a,b,c) 建立索引，查询条件使用 a&#x2F;ab&#x2F;abc 会走索引，使用 bc 不会走索引。<br>​	对(a,b,c,d) 建立索引，查询条件为a &#x3D; 1 and b &#x3D; 2 and c &gt; 3 and d &#x3D; 4 ，那么a、b和c三个字段能用到索引，而d无法使用索引。因为遇到了范围查询。</p>
<p>​	具体原因就是对(a,b)建立索引，那么在索引树中a是全局有序的，而b是全局无序，局部有序，只有在确定了a的值后，b才是有序的</p>
<p>​	因此如果直接查询b，索引是失效的。</p>
<h2 id="索引下推"><a href="#索引下推" class="headerlink" title="索引下推"></a>索引下推</h2><p>​	举例 <code>select name from table where level &gt; 10 and state = 1;</code></p>
<p>​	在进行这个查询后，由于最左匹配原则，使用索引后，会查询到所有level&gt;10的字段，如何交给SQL server再进行处理。拿到记录后，server会对这些记录一条一条回表查询，看是否满足state字段的条件。</p>
<p>​	而索引下推就是在交给server前先把一部分字段过滤掉，把state！&#x3D;1的记录过滤掉，再交给server，这样server拿到后不需要对每个记录都再回表一次，只需要进行查询即可。会<strong>大大减少回表操作。</strong></p>
<h2 id="索引的设计原则"><a href="#索引的设计原则" class="headerlink" title="索引的设计原则"></a>索引的设计原则</h2><ul>
<li>对于经常需要进行查询的字段建立索引</li>
<li>对于经常需要排序、联合操作的字段建立索引</li>
<li>避免给大字段建立索引，而是使用数据量较小的字段作为索引</li>
<li>索引不是越多越好，避免冗余索引，删除不用的索引</li>
<li>频繁进行增删改查的字段不建立索引</li>
<li>最左匹配原则</li>
</ul>
<h2 id="索引失效的情况"><a href="#索引失效的情况" class="headerlink" title="索引失效的情况"></a>索引失效的情况</h2><ul>
<li>组合索引不满足最左匹配原则</li>
<li>以%开头的like查询如%abc</li>
<li>发生隐式转换</li>
<li>查询索引列是否不等于某个值</li>
<li>对索引列进行计算</li>
<li>使用or连接，or后的字段没有索引</li>
</ul>
<p>​	</p>
<p>​	</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>MySQL</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL-日志</title>
    <url>/2023/03/12/%E6%95%B0%E6%8D%AE%E5%BA%93/MySQL-%E6%97%A5%E5%BF%97/</url>
    <content><![CDATA[<p><u><em><strong>很抽象的一章</strong></em></u></p>
<h2 id="redo-log（重做日志）"><a href="#redo-log（重做日志）" class="headerlink" title="redo log（重做日志）"></a>redo log（重做日志）</h2><p>​	InnoDB 独有的，用来记录innodb存储引擎的事务日志，不管事务是否提交都会记录下来，用于数据恢复，使得MySQL拥有了崩溃恢复的能力。</p>
<p>​	MySQL挂了或宕机了，InnoDB会使用redo log恢复数据，保证数据的持久性与完整性。</p>
<p>​	当在MySQL中查询数据时，数据库会从硬盘把一页的数据加载出来，叫做数据页，然后放到Buffer Pool中。后续的查询操作都是从这个Pool中找，没有命中再去硬盘加载，减少硬盘IO开销。</p>
<p>​	更新操作也是对这个Pool进行修改，数据库会把对数据页的修改记录到<strong>重做日志缓存(redo log buffer)<strong>中，接着把这些缓存</strong>刷盘</strong>到redo log文件中。</p>
<p>​	理想情况，事务一提交就会进行刷盘操作，但实际上，刷盘的时机是根据策略来进行的。</p>
<h3 id="刷盘"><a href="#刷盘" class="headerlink" title="刷盘"></a>刷盘</h3><p>​	对于刷盘操作，InnoDB设置了一个参数叫做<code>innodb_flush_log_at_trx_commit</code>，用于设定刷盘的策略，对于这个参数，有以下几种赋值情况:</p>
<ul>
<li>0:每次事务提交<strong>都不进行</strong>刷盘操作</li>
<li>1:每次事务提交<strong>都将进行</strong>刷盘操作(default)</li>
<li>2:每次事务提交都只把redo log buffer<strong>写入文件系统缓存(page cache)中</strong></li>
</ul>
<p>​	<code>InnoDB</code> 存储引擎有一个后台线程，每隔<code>1</code> 秒，就会把 <code>redo log buffer</code> 中的内容写到文件系统缓存（<code>page cache</code>），然后调用 <code>fsync</code> 刷盘。</p>
<p>​	在这里要讲一下redo写入磁盘的全流程：</p>
<ol>
<li>将redo日志写入redo log buffer当中（此时仍然再内存中）</li>
<li>redo log buffer写入page cache（操作系统里的页，系统会自动刷到磁盘，或者调用fsync函数手动刷盘）</li>
<li>将page cache中的数据刷到磁盘中的redo file当中</li>
</ol>
<p>​	<strong>那么就可以用这个过程来解释上述几个赋值的情况：</strong></p>
<p>​	0：提交事务后不立即写入page cache，也不写到磁盘里，完全交给后台线程处理。这种情况十分危险但性能好</p>
<p>​	1：提交事务后立即写入page cache，然后调用fsync让他立即写入磁盘，性能最差但最安全</p>
<p>​	2：提交事务后只是写入page cache。如果这个时候mysql挂了但操作系统没挂，还可以调度fsync落盘</p>
<p>​	除了后台线程每秒<code>1</code>次的轮询操作，还有一种情况，当 <code>redo log buffer</code> 占用的空间即将达到 <code>innodb_log_buffer_size</code> 一半的时候，后台线程会主动刷盘。</p>
<p>​	</p>
<h3 id="存储形式"><a href="#存储形式" class="headerlink" title="存储形式"></a>存储形式</h3><p>​	硬盘上存储的redo log日志文件不止一个，而是一个日志文件组的形式出现，每个redo日志文件大小一样。比如可以配置为一组<code>4</code>个文件，每个文件的大小是 <code>1GB</code>，整个 <code>redo log</code> 日志文件组可以记录<code>4G</code>的内容。它采用的是环形数组形式，从头开始写，写到末尾又回到头循环写，如下图所示。</p>
<p>​	<img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/10.png"></p>
<p>​	日志文件组中有两个重要的属性：</p>
<ul>
<li><strong>write pos</strong> 当前记录的位置，一边写一边后移</li>
<li><strong>checkpoint</strong>是当前要擦除的位置，也是往后推移</li>
</ul>
<p>​	每次刷盘 redo log 记录到日志文件组中，write pos 位置就会后移更新。</p>
<p>​	每次 MySQL 加载日志文件组恢复数据时，会清空加载过的 redo log 记录，并把 checkpoint 后移更新。</p>
<p>​	write pos 和 checkpoint 之间的还空着的部分可以用来写入新的 redo log 记录。</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/11.png"></p>
<p>​	如果 write pos 追上 checkpoint ，表示日志文件组满了，这时候不能再写入新的 redo log 记录，MySQL 得停下来，清空一些记录，把 checkpoint 推进一下。</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/12.png"></p>
<h2 id="Undo-log-回滚日志"><a href="#Undo-log-回滚日志" class="headerlink" title="Undo log(回滚日志)"></a>Undo log(回滚日志)</h2><p>​	除了记录redo log 外，当进行数据修改时还会记录undo log ， undo log 用于数<strong>据的撤回操作</strong>，它保留了记录修改前的内容。通过undo log 可以实现事务回滚，并且可以根据undo log <strong>回溯到某个特定的版本的数据</strong>，实现MVCC。</p>
<h2 id="Binlog（归档日志）"><a href="#Binlog（归档日志）" class="headerlink" title="Binlog（归档日志）"></a>Binlog（归档日志）</h2><p>​	二进制日志（binary log ），记录对 Mysql 数据库执行的所有 <strong>更改操作</strong>，包括 表<strong>结构的变更 和 表数据的修改</strong> 等，像 select 这种查询是不会记录 binlog 日志的。</p>
<p>​	binlog 日志采用 追加写 的方式写文件，一个文件写满后新写一个文件，仅在 事务提交前 进行一次写入。</p>
<p>​	生成的 binlog 日志文件可以用于 备份恢复、主从复制 以及 数据审计 等用途。</p>
<p>​	Binlog和 redo log的区别在于：</p>
<p>​	redo log 它是<strong>物理日志</strong>，记录内容是“在某个数据页上做了什么修改”，属于 InnoDB 存储引擎。而 binlog 是<strong>逻辑日志</strong>，记录内容是语句的原始逻辑，类似于“给 ID&#x3D;2 这一行的 c 字段加 1”，属于MySQL Server 层。</p>
<h3 id="记录格式"><a href="#记录格式" class="headerlink" title="记录格式"></a>记录格式</h3><h5 id="statement格式"><a href="#statement格式" class="headerlink" title="statement格式"></a>statement格式</h5><p>​	在statement格式里，binlog记录的是SQL语句原文，同步数据时会执行记录的SQL语句</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/02-20220305234738688.png"></p>
<p>​	但会存在一个问题，上面那个now()记录的是当前的系统时间，而写道binlog里的话，时间不会被记录，就会出现不一致的问题</p>
<h5 id="row格式"><a href="#row格式" class="headerlink" title="row格式"></a>row格式</h5><p>​	row就是解决这个问题，记录的内容就不只是SQL语句，还包括操作的具体数据</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/03-20220305234742460.png"></p>
<p>​	但是这种格式，需要更大的容量来记录，比较占用空间，恢复与同步时会更消耗<code>IO</code>资源，影响执行速度。</p>
<h5 id="mixed格式"><a href="#mixed格式" class="headerlink" title="mixed格式"></a>mixed格式</h5><p>​	所以就有了一种折中的方案，指定为<code>mixed</code>，记录的内容是前两者的混合。</p>
<p>​	<code>MySQL</code>会判断这条<code>SQL</code>语句是否可能引起数据不一致，如果是，就用<code>row</code>格式，否则就用<code>statement</code>格式。</p>
<h3 id="写入机制"><a href="#写入机制" class="headerlink" title="写入机制"></a>写入机制</h3><p>​	事务执行过程中，先把日志写到<code>binlog cache</code>，事务提交的时候，再把<code>binlog cache</code>写到<code>binlog</code>文件中。</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/04-20220305234747840.png"></p>
<ul>
<li><strong>上图的 write，是指把日志写入到文件系统的 page cache，并没有把数据持久化到磁盘，所以速度比较快</strong></li>
<li><strong>上图的 fsync，才是将数据持久化到磁盘的操作</strong></li>
</ul>
<p>​	参数<code>sync_binlog</code>控制write和fsync的时机，默认是<code>0</code>。</p>
<ul>
<li>0：每次提交事务都只write，由系统自行判断fsync时机</li>
<li>1：每次提交事务都fsync</li>
<li>N：每次提交事务都write，但积累N个事务后才fsync</li>
</ul>
<h2 id="两阶段提交"><a href="#两阶段提交" class="headerlink" title="两阶段提交"></a>两阶段提交</h2><p>​	在主从复制模式下，会开启 binlog 日志。此时，主库提交事务时，既要写二进制日志，还要写重做日志，mysql 需要保证两个操作的原子性。假如它们不满足原子性，比如先写完二进制日志后，如果 mysql 实例发生宕机，重启后，由于 redo log 日志没有记录的原因，在做数据恢复后主库将丢失这个数据更新，而从库却根据 binlog 日志进行了重放，最终导致主从不一致。</p>
<p>​	我们以update语句为例，假设id&#x3D;2的记录，字段c值是0，把字段c值更新成1，SQL语句为update T set c&#x3D;1 where id&#x3D;2。</p>
<p>​	假设执行过程中写完redo log日志后，binlog日志写期间发生了异常，这时候<code>binlog</code>里面没有对应的修改记录。因此，之后用<code>binlog</code>日志恢复数据时，就会少这一次更新，恢复出来的这一行<code>c</code>值是<code>0</code>，而原库因为<code>redo log</code>日志恢复，这一行<code>c</code>值是<code>1</code>，最终数据不一致。</p>
<p>​	mysql 结合 内部 XA 事务 和 两阶段提交方案 来实现这两个操作的原子性，看看具体流程：</p>
<p>1、prepare 阶段：事务提交时，先写 redo log，同时记录 XA 事务的 ID (XID)，标记为 prepare 状态；</p>
<p>2、写入 binlog 日志，同时记录 XID；</p>
<p>3、commit 阶段：再次写 redo log，标记为 commit 状态。</p>
<p><img src="https://guide-blog-images.oss-cn-shenzhen.aliyuncs.com/github/javaguide/05-20220305234937243.png"></p>
<p>​	我们看看它是如果通过以上步骤实现原子性的：</p>
<p>​	在 mysql 使用 redo log 做数据恢复时，如果发现 redo log 处于 commit 状态，则表示 binlog 一定落盘了，可以直接恢复；</p>
<p>​	如果发现 redo log 处于 prepare 状态，就要根据 XID 和 binlog 日志来判断：</p>
<ul>
<li><p>如果在 binlog 找不到这个 XID，说明还没有同步 binlog，回滚事务；</p>
</li>
<li><p>反之说明两个日志都已同步成功，提交事务。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>MySQL</tag>
        <tag>日志</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL-事务</title>
    <url>/2023/03/13/%E6%95%B0%E6%8D%AE%E5%BA%93/MySQL-%E4%BA%8B%E5%8A%A1/</url>
    <content><![CDATA[<h2 id="数据库事务"><a href="#数据库事务" class="headerlink" title="数据库事务"></a>数据库事务</h2><h3 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h3><p>​	保证多个对数据库的操作构成一个逻辑上的整体，<strong>要么全部执行成功，要么全部不执行</strong>。</p>
<h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>A原子性</strong>：事务是最小的执行单位，不允许分割。原子性确保动作要么全部完成，要么全部不起作用。</li>
<li><strong>C一致性</strong>：执行事务前后，数据保持一致。</li>
<li><strong>I隔离性</strong>：并发访问时，一个用户的事务不被其他事务所干扰。</li>
<li><strong>D持久性</strong>：事务提交后对数据库中的改变是持久的，即使数据库发生故障也不应该有影响。</li>
</ul>
<h2 id="并发事务带来的问题"><a href="#并发事务带来的问题" class="headerlink" title="并发事务带来的问题"></a>并发事务带来的问题</h2><h3 id="脏读-Dirty-read"><a href="#脏读-Dirty-read" class="headerlink" title="脏读(Dirty read)"></a>脏读(Dirty read)</h3><p>​	一个事务读取数据并且对数据进行了修改，<strong>这个修改对其他事务来说是可见的</strong>（<u>这里对应的是隔离级别中的读未提交，也就是必须处于读未提交的隔离级别才行）</u>，即使当前事务没有提交。这时另外一个事务读取了这个还未提交的数据，但第一个事务突然回滚，导致数据并没有被提交到数据库，那第二个事务读取到的就是脏数据，这也就是脏读的由来。</p>
<p>​	简单来说就是事务1读取了数据A，对他做了修改后，该修改后的数据对其他事务来说是可见的。事务2读取了修改后的数据A并进行操作。此时事务1突然回滚了，数据并没有提交到数据库，此时事务2读的就是脏数据。</p>
<h3 id="丢失修改-Lost-to-modify"><a href="#丢失修改-Lost-to-modify" class="headerlink" title="丢失修改(Lost to modify)"></a>丢失修改(Lost to modify)</h3><p>​	事务1读取了数据A，事务2也读取了数据A。事务1修改了数据A，事务2也修改了数据A。事务1提交事务，将修改后的A写入数据库，事务2也提交，此时就覆盖了事务1的修改，导致事务1的修改丢失。</p>
<h3 id="不可重复读-Unrepeatable-read"><a href="#不可重复读-Unrepeatable-read" class="headerlink" title="不可重复读(Unrepeatable read)"></a>不可重复读(Unrepeatable read)</h3><p>​	事务1读取了数据A，事务2也读取了数据A。事务1对数据A做了修改并提交，此时事务2再次读取数据A，但发现两次读取结果不同。</p>
<h3 id="幻读-phantom-read"><a href="#幻读-phantom-read" class="headerlink" title="幻读(phantom read)"></a>幻读(phantom read)</h3><p>​	事务 2 读取某个范围的数据，事务 1 在这个范围插入了新的数据，事务 2 再次读取这个范围的数据发现相比于第一次读取的结果多了新的数据。</p>
<h2 id="并发事务的控制方式"><a href="#并发事务的控制方式" class="headerlink" title="并发事务的控制方式"></a>并发事务的控制方式</h2><h3 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h3><p>MySQL中主要是通过<strong>读写锁</strong>来实现并发控制。</p>
<ul>
<li><strong>共享锁（S锁）</strong>：又称读锁，事务在读取记录时获取共享锁，允许多个事务同时获取（锁兼容）。</li>
<li><strong>排他锁（X锁）</strong>：又称写锁，事务在修改记录时获取排他锁，不允许多个事务同时获取。如果一个记录已经被加了排他锁，那其他事务不能再对这条记录加任何类型的锁（锁不兼容）。</li>
</ul>
<p>几个注意点：</p>
<ol>
<li>共享锁只能用于表记，排他锁用于行级。共享锁保证了其他事务不能写，排他锁保证了其他事务不能读。</li>
<li><strong>加了共享锁的对象可以继续加共享锁，但不能加排他锁</strong>。加了排他锁后不能再加任何锁。</li>
</ol>
<h3 id="MVCC"><a href="#MVCC" class="headerlink" title="MVCC"></a>MVCC</h3><p>​	多版本并发控制方法。<strong>对一份数据会存储多个版本</strong>，通过事务的可见性来保证事务能看到自己应该看到的版本。</p>
<h2 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h2><ul>
<li><strong>读取未提交</strong>：最低的隔离级别，允许事务读取其他事务尚未提交的数据变更。会导致脏读、幻读、不可重复读</li>
<li><strong>读取已提交</strong>（基于MVCC）：允许读取并发事务已经提交的数据，可以阻止脏读，但是幻读和不可重复读仍有可能发生</li>
<li><strong>可重复读</strong>（基于MVCC）：对同一字段的多次读取结果都是一致的，除非数据是被本身事务自己所修改，可以阻止脏读和不可重复读，但幻读仍有可能发生。<strong>是MySQL的默认隔离级别</strong></li>
<li><strong>可串行化</strong>（基于锁实现）：最高的隔离级别，所有的事物依次逐个进行，完全没有任何干扰</li>
</ul>
<p>​	对隔离级别的理解是，数据库对事务的限制或者允许事务处于的状态。如读取未提交就是允许事务读取未提交，读取已提交就是要求事务只能读取已提交。</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>MySQL</tag>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring和MyBatis</title>
    <url>/2023/03/14/Java/Java-Spring%E5%92%8CMyBatis/</url>
    <content><![CDATA[<p><em><strong><u>学Java哪有不疯的，哈哈</u></strong></em></p>
<h1 id="Spring"><a href="#Spring" class="headerlink" title="Spring"></a>Spring</h1><h2 id="IoC"><a href="#IoC" class="headerlink" title="IoC"></a>IoC</h2><p>​	控制反转是一种设计思想。就是<strong>将原本在程序中手动创建对象的控制权交由Spring框架来管理</strong>。最直接的体现就是不用去new一个对象了，而是由Spring容器来管理一个叫<strong>bean</strong>的东西，控制它的整个生命周期。Spring通过Java的<strong>反射</strong>机制来实现对其他对象的控制，包括初始化、创建、销毁等，解放手动创建对象的过程，同时降低类之间的耦合度。</p>
<h3 id="IoC的好处"><a href="#IoC的好处" class="headerlink" title="IoC的好处"></a>IoC的好处</h3><p>​	资源不由使用资源者管理，而有不使用资源的第三方管理。</p>
<ul>
<li>资源集中管理，实现资源的可配置和易管理</li>
<li>降低了使用资源双方的依赖程度，也就是耦合度</li>
</ul>
<h3 id="DI（依赖注入）"><a href="#DI（依赖注入）" class="headerlink" title="DI（依赖注入）"></a>DI（依赖注入）</h3><p>​	IOC最常见的实现方式是依赖注入，指的是在Spring创建对象的过程中，把对象依赖的属性注入到对象中。</p>
<p>​	在SpringBoot中可以使用@Autowired注解进行依赖注入</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@RequestMapping(&quot;test&quot;)</span><br><span class="hljs-meta">@RestController</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestController</span> &#123;<br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> Cat cat;<br>    <span class="hljs-meta">@RequestMapping(&quot;say&quot;)</span><br>    <span class="hljs-keyword">private</span> String <span class="hljs-title function_">login</span><span class="hljs-params">()</span>&#123;<br>        <span class="hljs-keyword">return</span> cat.sayHello();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>​	也可以使用set的方式，在xml文件中注入</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//（1）创建类，定义属性和对应的 set 方法</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Book</span> &#123;<br>	<span class="hljs-comment">//创建属性</span><br>	<span class="hljs-keyword">private</span> String bname;<br>	<span class="hljs-keyword">private</span> String bauthor;<br>	<span class="hljs-comment">//创建属性对应的 set 方法</span><br>	<span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setBname</span><span class="hljs-params">(String bname)</span> &#123;<br>		<span class="hljs-built_in">this</span>.bname = bname;<br>	&#125;<br>	<span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setBauthor</span><span class="hljs-params">(String bauthor)</span> &#123;<br>		<span class="hljs-built_in">this</span>.bauthor = bauthor;<br>	&#125; <br>&#125;<br><span class="hljs-comment">//（2）在 spring 配置文件配置对象创建，配置属性注入</span><br>&lt;bean id=<span class="hljs-string">&quot;book&quot;</span> class=<span class="hljs-string">&quot;com.atguigu.spring5.Book&quot;</span>&gt;<br>	<span class="hljs-comment">//使用 property 完成属性注入</span><br>	<span class="hljs-comment">//name：类里面属性名称</span><br>	<span class="hljs-comment">//value：向属性注入的值</span><br>	&lt;property name=<span class="hljs-string">&quot;bname&quot;</span> value=<span class="hljs-string">&quot;西游记&quot;</span>&gt;&lt;/property&gt;<br>	&lt;property name=<span class="hljs-string">&quot;bauthor&quot;</span> value=<span class="hljs-string">&quot;吴承恩&quot;</span>&gt;&lt;/property&gt;<br>&lt;/bean&gt;<br><br></code></pre></td></tr></table></figure>

<p>​	有参构造也是可以的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//（1）创建类，定义属性，创建属性对应有参数构造方法</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Book</span> &#123;<br>	<span class="hljs-comment">//属性</span><br>	<span class="hljs-keyword">private</span> String bname;<br>	<span class="hljs-keyword">private</span> String bauthor;<br>	<span class="hljs-comment">//有参数构造</span><br>	<span class="hljs-keyword">public</span> <span class="hljs-title function_">Orders</span><span class="hljs-params">(String bname,String bauthor)</span> &#123;<br>		<span class="hljs-built_in">this</span>.bname= bname;<br>		<span class="hljs-built_in">this</span>.bauthor= bauthor;<br>	&#125; <br>&#125;<br><span class="hljs-comment">//（2）在 spring 配置文件中进行配置</span><br>&lt;bean id=<span class="hljs-string">&quot;orders&quot;</span> class=<span class="hljs-string">&quot;com.atguigu.spring5.Orders&quot;</span>&gt;<br>	&lt;constructor-arg name=<span class="hljs-string">&quot;bname&quot;</span> value=<span class="hljs-string">&quot;三国演义&quot;</span>&gt;&lt;/constructor-arg&gt;<br>	&lt;constructor-arg name=<span class="hljs-string">&quot;bauthor&quot;</span> value=<span class="hljs-string">&quot;罗贯中&quot;</span>&gt;&lt;/constructor-arg&gt;<br>&lt;/bean&gt;<br></code></pre></td></tr></table></figure>

<h3 id="Bean"><a href="#Bean" class="headerlink" title="Bean"></a>Bean</h3><p>​	Bean代指的就是那些被IoC容器所管理的对象。我们需要告诉IoC容器帮助我们管理哪些对象，这个是通过配置元数据来定义的。配置元数据可以是 XML 文件、注解或者 Java 配置类。</p>
<h4 id="创建Bean的方式"><a href="#创建Bean的方式" class="headerlink" title="创建Bean的方式"></a>创建Bean的方式</h4><ul>
<li>直接在xml文件中声明一个类为Bean</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- Constructor-arg with &#x27;value&#x27; attribute --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;...&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;...&quot;</span>&gt;</span><br>   <span class="hljs-tag">&lt;<span class="hljs-name">constructor-arg</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;...&quot;</span>/&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><br></code></pre></td></tr></table></figure>

<ul>
<li><p>使用@Component,@Service,@Controller,@Repository注解</p>
<ul>
<li><strong>@Component</strong> : 侧重于通用的Bean类</li>
<li><strong>@Service</strong>：标识该类用于业务逻辑</li>
<li><strong>@Controller</strong>：标识该类为Spring MVC的控制器类</li>
<li><strong>@Repository</strong>: 对应持久即Dao层，只有属性和Setter,Getter</li>
</ul>
</li>
<li><p>使用@Bean注解,这种方式用在Spring Boot 应用中</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//@Configuration 标识这是一个Spring Boot 配置类</span><br><span class="hljs-comment">//其将会扫描该类中是否存在@Bean 注解的方法</span><br><span class="hljs-comment">//比如如下代码，将会创建User对象并放入容器中。</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserConfiguration</span>&#123;<br>     <br>      <span class="hljs-meta">@Bean</span>　　　 <br>      <span class="hljs-meta">@ConditionalOnBean(Location.class)</span><br>      <span class="hljs-keyword">public</span> User <span class="hljs-title function_">user</span><span class="hljs-params">()</span>&#123;<br>           <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">User</span>();<br>      &#125;<br>      <br>&#125;<br></code></pre></td></tr></table></figure>

<ul>
<li>使用注解@Import,也会创建对象并注入容器中</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Import(User.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MicroblogUserWebApplication</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String args[])</span> &#123;<br>        SpringApplication.run(MicroblogUserWebApplication.class, args);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="Component-和-Bean-的区别是什么？"><a href="#Component-和-Bean-的区别是什么？" class="headerlink" title="@Component 和 @Bean 的区别是什么？"></a>@Component 和 @Bean 的区别是什么？</h4><ul>
<li>@Component注解作用于类，@Bean注解作用于方法</li>
<li>@Component通常是通过类路径扫描来自动侦测以及自动装配到Spring容器中。@Bean注解通常是在标有该注解的方法中定义产生这个bean，告诉Spring这是某个类的实例。</li>
<li>@bean注解比@Component注解的自定义性更强，比如引用第三方库中的类时只能用@Bean</li>
</ul>
<h4 id="注入Bean的注解有哪些？"><a href="#注入Bean的注解有哪些？" class="headerlink" title="注入Bean的注解有哪些？"></a>注入Bean的注解有哪些？</h4><p>@Autowired @Resource @Inject</p>
<h4 id="Autowired和-Resource的区别是什么"><a href="#Autowired和-Resource的区别是什么" class="headerlink" title="@Autowired和@Resource的区别是什么"></a>@Autowired和@Resource的区别是什么</h4><ul>
<li>Autowired属于<strong>Spring内置</strong>的注解，默认的注入方式为<strong>byType</strong>，也就是<strong>按类型匹配</strong>，优先根据接口类型去匹配并注入Bean。但如果出现某个类型下有好几个实现类的情况的话就会出错，这种情况下就要再加个@Qualifier注解来显式指定名称。</li>
<li>Resource属于<strong>JDK提供</strong>的注解，默认注入方式为byName，有两个比较重要的属性:name(名称)，type(类型)。可以通过name属性来显示指定名称。</li>
</ul>
<h4 id="Bean的作用域有哪些？"><a href="#Bean的作用域有哪些？" class="headerlink" title="Bean的作用域有哪些？"></a>Bean的作用域有哪些？</h4><p>​	通过加个@scope注解来配置bean的作用域</p>
<ul>
<li><strong>singleton</strong>：容器中只有唯一的bean实例。默认都是单例的。</li>
<li><strong>prototype</strong>：每次获取都会创建一个新的 bean 实例。</li>
<li><strong>request</strong> ：每次HTTP请求都会产生一个新的bean，该bean仅在当前的HTTP request中有效。</li>
<li><strong>session</strong>：每次来自新的session的HTTP请求都会产生一个新的bean，该bean仅在当前的HTTP session内有效。</li>
<li><strong>application&#x2F;global-session</strong>：每个Web应用在启动时创建一个Bean，该Bean仅在当前应用启动时间内有效。</li>
<li><strong>websocket</strong>：每一次websocket会话产生一个新的bean。</li>
</ul>
<h4 id="单例-Bean-的线程安全问题了解吗？"><a href="#单例-Bean-的线程安全问题了解吗？" class="headerlink" title="单例 Bean 的线程安全问题了解吗？"></a>单例 Bean 的线程安全问题了解吗？</h4><p>​	单例Bean存在线程问题，主要是因为当多个线程操作同一个对象的时候是存在资源竞争的。</p>
<p>​	解决方法：</p>
<ol>
<li>在Bean中尽量避免定义可变的成员变量。</li>
<li>在类中定义ThreadLocal成员变量，将需要的可变成员变量保存在ThreadLocal中。</li>
</ol>
<p>​	但一般的Bean实际上都是无状态的，如Dao，Service，所以大部分情况下Bean是线程安全的。</p>
<h2 id="AOP"><a href="#AOP" class="headerlink" title="AOP"></a>AOP</h2><p>​	将那些与具体业务实现无关，却被业务共同调用的部分封装起来，减少系统的重复代码，是开闭原则的体现。比如在一些业务中需要在执行之前验证用户权限，验证权限的实现逻辑其实是一样的，就可以把他们抽象成一个切面，然后由Spring统一注入到业务代码中。</p>
<h3 id="AOP有哪些实现方式？"><a href="#AOP有哪些实现方式？" class="headerlink" title="AOP有哪些实现方式？"></a>AOP有哪些实现方式？</h3><h4 id="静态代理"><a href="#静态代理" class="headerlink" title="静态代理"></a>静态代理</h4><p>​	基于XML配置的Spring AOP，采用声明的方式实现，在XML文件中配置，大致步骤为：配置文件中配置pointcut，在java中编写实际的aspect类，针对切入点进行相关的业务处理。</p>
<p>​	通俗来说就是在类中写好方法，然后在xml里配置切点，切面和通知等。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"> <span class="hljs-tag">&lt;<span class="hljs-name">aop:config</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">aop:aspect</span> <span class="hljs-attr">ref</span>=<span class="hljs-string">&quot;aspectBean&quot;</span>&gt;</span><br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:pointcut</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;pointcut&quot;</span> <span class="hljs-attr">expression</span>=<span class="hljs-string">&quot;execution(* com.spring.service.impl.UserManagerServiceImpl..*(..))&quot;</span>/&gt;</span><br>     <br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:before</span> <span class="hljs-attr">method</span>=<span class="hljs-string">&quot;doBefore&quot;</span> <span class="hljs-attr">pointcut-ref</span>=<span class="hljs-string">&quot;pointcut&quot;</span>/&gt;</span> <br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:after-returning</span> <span class="hljs-attr">method</span>=<span class="hljs-string">&quot;doAfterReturning&quot;</span> <span class="hljs-attr">pointcut-ref</span>=<span class="hljs-string">&quot;pointcut&quot;</span> <span class="hljs-attr">returning</span>=<span class="hljs-string">&quot;result&quot;</span>/&gt;</span><br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:after</span> <span class="hljs-attr">method</span>=<span class="hljs-string">&quot;doAfter&quot;</span> <span class="hljs-attr">pointcut-ref</span>=<span class="hljs-string">&quot;pointcut&quot;</span> /&gt;</span> <br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:around</span> <span class="hljs-attr">method</span>=<span class="hljs-string">&quot;doAround&quot;</span> <span class="hljs-attr">pointcut-ref</span>=<span class="hljs-string">&quot;pointcut&quot;</span>/&gt;</span> <br>     <span class="hljs-tag">&lt;<span class="hljs-name">aop:after-throwing</span> <span class="hljs-attr">method</span>=<span class="hljs-string">&quot;doAfterThrowing&quot;</span> <span class="hljs-attr">pointcut-ref</span>=<span class="hljs-string">&quot;pointcut&quot;</span> <span class="hljs-attr">throwing</span>=<span class="hljs-string">&quot;ex&quot;</span>/&gt;</span><br>   <span class="hljs-tag">&lt;/<span class="hljs-name">aop:aspect</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">aop:config</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h4 id="动态代理"><a href="#动态代理" class="headerlink" title="动态代理"></a>动态代理</h4><p>​	主要就是使用使用aspectJ的注解来进行创建，通过加上**@Aspect<strong>注解表明这是个切面，在默认构造函数上加上</strong>@PointCut<strong>表明切入点表达式，然后在那些通知方法上加上</strong>@Before**,<strong>@Around</strong>这种注解声明在什么时候使用这个通知。</p>
<p>​	这种方式也要在xml文件中<strong>声明spring对@AspectJ的支持</strong>并<strong>声明通知类</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.spring.aop;<br><br><span class="hljs-keyword">import</span> org.aspectj.lang.JoinPoint;<br><span class="hljs-keyword">import</span> org.aspectj.lang.ProceedingJoinPoint;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.After;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.AfterReturning;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.AfterThrowing;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.Around;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.Aspect;<br><span class="hljs-keyword">import</span> org.aspectj.lang.annotation.Before;<br><br><span class="hljs-meta">@Aspect</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AopAspectJ</span> &#123;<br>    <br>    <span class="hljs-comment">/**  </span><br><span class="hljs-comment">     * 必须为final String类型的,注解里要使用的变量只能是静态常量类型的  </span><br><span class="hljs-comment">     */</span>  <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String EDP=<span class="hljs-string">&quot;execution(* com.spring.service.impl.UserManagerServiceImpl..*(..))&quot;</span>;<br>    <br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 切面的前置方法 即方法执行前拦截到的方法</span><br><span class="hljs-comment">      * 在目标方法执行之前的通知</span><br><span class="hljs-comment">      * <span class="hljs-doctag">@param</span> jp</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@Before(EDP)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doBefore</span><span class="hljs-params">(JoinPoint jp)</span>&#123;<br>        <br>        System.out.println(<span class="hljs-string">&quot;=========执行前置通知==========&quot;</span>);<br>    &#125;<br>    <br>    <br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 在方法正常执行通过之后执行的通知叫做返回通知</span><br><span class="hljs-comment">      * 可以返回到方法的返回值 在注解后加入returning </span><br><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> jp</span><br><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> result</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@AfterReturning(value=EDP,returning=&quot;result&quot;)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doAfterReturning</span><span class="hljs-params">(JoinPoint jp,String result)</span>&#123;<br>        System.out.println(<span class="hljs-string">&quot;===========执行后置通知============&quot;</span>);<br>    &#125;<br>    <br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 最终通知：目标方法调用之后执行的通知（无论目标方法是否出现异常均执行）</span><br><span class="hljs-comment">      * <span class="hljs-doctag">@param</span> jp</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@After(value=EDP)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doAfter</span><span class="hljs-params">(JoinPoint jp)</span>&#123;<br>        System.out.println(<span class="hljs-string">&quot;===========执行最终通知============&quot;</span>);<br>    &#125;<br>    <br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 环绕通知：目标方法调用前后执行的通知，可以在方法调用前后完成自定义的行为。</span><br><span class="hljs-comment">      * <span class="hljs-doctag">@param</span> pjp</span><br><span class="hljs-comment">     * <span class="hljs-doctag">@return</span></span><br><span class="hljs-comment">     * <span class="hljs-doctag">@throws</span> Throwable</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@Around(EDP)</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">doAround</span><span class="hljs-params">(ProceedingJoinPoint pjp)</span> <span class="hljs-keyword">throws</span> Throwable&#123;<br><br>        System.out.println(<span class="hljs-string">&quot;======执行环绕通知开始=========&quot;</span>);<br>        <span class="hljs-comment">// 调用方法的参数</span><br>        Object[] args = pjp.getArgs();<br>        <span class="hljs-comment">// 调用的方法名</span><br>        <span class="hljs-type">String</span> <span class="hljs-variable">method</span> <span class="hljs-operator">=</span> pjp.getSignature().getName();<br>        <span class="hljs-comment">// 获取目标对象</span><br>        <span class="hljs-type">Object</span> <span class="hljs-variable">target</span> <span class="hljs-operator">=</span> pjp.getTarget();<br>        <span class="hljs-comment">// 执行完方法的返回值</span><br>        <span class="hljs-comment">// 调用proceed()方法，就会触发切入点方法执行</span><br>        Object result=pjp.proceed();<br>        System.out.println(<span class="hljs-string">&quot;输出,方法名：&quot;</span> + method + <span class="hljs-string">&quot;;目标对象：&quot;</span> + target + <span class="hljs-string">&quot;;返回值：&quot;</span> + result);<br>        System.out.println(<span class="hljs-string">&quot;======执行环绕通知结束=========&quot;</span>);<br>        <span class="hljs-keyword">return</span> result;<br>    &#125;<br>    <br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 在目标方法非正常执行完成, 抛出异常的时候会走此方法</span><br><span class="hljs-comment">      * <span class="hljs-doctag">@param</span> jp</span><br><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> ex</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@AfterThrowing(value=EDP,throwing=&quot;ex&quot;)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doAfterThrowing</span><span class="hljs-params">(JoinPoint jp,Exception ex)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;===========执行异常通知============&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="AspectJ-定义的通知类型有哪些？"><a href="#AspectJ-定义的通知类型有哪些？" class="headerlink" title="AspectJ 定义的通知类型有哪些？"></a>AspectJ 定义的通知类型有哪些？</h3><ul>
<li>before:目标对象的方法调用之前触发</li>
<li>after:目标对象的方法调用之后触发</li>
<li>afterReturning:目标对象的方法调用完成，在返回结果值之后触发</li>
<li>afterThrowing:目标对象抛出异常后触发</li>
<li>around：可以任意的在目标对象的方法调用前后触发</li>
</ul>
<h3 id="Spring-AOP的实现原理"><a href="#Spring-AOP的实现原理" class="headerlink" title="Spring AOP的实现原理"></a>Spring AOP的实现原理</h3><p>​	通过<strong>动态代理</strong>实现。如果我们为Spring的某个bean配置了切面，那么Spring在创建这个bean的时候，实际上创建的是这个bean的一个<strong>代理对象</strong>，后续对bean中方法的调用，实际上<strong>调用的是代理类重写的代理方法</strong>，AOP使用了两种动态代理，分别是<strong>JDK的动态代理</strong>和<strong>CGLib的动态代理</strong>。</p>
<h3 id="JDK动态代理和CGLIB动态代理的区别？"><a href="#JDK动态代理和CGLIB动态代理的区别？" class="headerlink" title="JDK动态代理和CGLIB动态代理的区别？"></a>JDK动态代理和CGLIB动态代理的区别？</h3><ol>
<li>JDK动态代理使用jdk中的类Proxy来创建代理对象，使用反射技术来实现，不需要导入其他的依赖。CGLIB需要导入相关依赖，使用字节码增强技术来实现。</li>
<li>当目标类实现了接口的时候，默认使用JDK动态代理方式来增强方法，没有实现接口的时候使用CGLIB动态代理方法来增强方法。</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Java</tag>
        <tag>Spring</tag>
        <tag>MyBatis</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis知识点及考点整理</title>
    <url>/2023/03/14/%E6%95%B0%E6%8D%AE%E5%BA%93/Redis%E7%9F%A5%E8%AF%86%E7%82%B9%E5%8F%8A%E8%80%83%E7%82%B9%E6%95%B4%E7%90%86/</url>
    <content><![CDATA[<p><u><em><strong>妈的，就会这些，其他的看也不看</strong></em></u></p>
<h2 id="Redis是什么"><a href="#Redis是什么" class="headerlink" title="Redis是什么"></a>Redis是什么</h2><p>​	是一个使用C语言编写的，高性能非关系型的<strong>键值对数据库</strong>。由于数据<strong>存储在内存</strong>中，Redis的<strong>读写速度非常快</strong>，被广泛应用于<strong>缓存方向</strong>。Redis存储的是<strong>KV键值对</strong>，同时<strong>可以将数据写入磁盘</strong>中，保证了<strong>数据的安全</strong>不丢失。</p>
<h2 id="Redis的优缺点"><a href="#Redis的优缺点" class="headerlink" title="Redis的优缺点"></a>Redis的优缺点</h2><p><strong>优点：</strong></p>
<ul>
<li><strong>基于内存操作</strong>，读写速度快。</li>
<li><strong>支持多种数据类型</strong>，包括String，List等</li>
<li>支持持久化。支持<strong>RDB</strong>和<strong>AOF</strong>两种持久化机制，可以有效地避免数据丢失问题。</li>
<li>支持<strong>事务</strong>。但事务不常使用。</li>
<li>支持<strong>主从复制</strong>。</li>
<li>命令的处理是<strong>单线程</strong>的。</li>
</ul>
<p><strong>缺点</strong>：</p>
<ul>
<li>对结构化查询的支持较差</li>
<li>容量受到物理内存的限制</li>
<li>较难支持在线扩容</li>
</ul>
<h2 id="Redis为什么这么快"><a href="#Redis为什么这么快" class="headerlink" title="Redis为什么这么快"></a>Redis为什么这么快</h2><ul>
<li><strong>基于内存操作</strong>：内存的读写速度是硬盘的上千倍</li>
<li><strong>I&#x2F;O多路复用模型</strong>：采用IO多路复用技术，监听多个Socket并利用单线程事务轮询</li>
<li><strong>高效的数据结构</strong>：对每种数据结构都做了底层优化</li>
</ul>
<h2 id="Redis的线程模型"><a href="#Redis的线程模型" class="headerlink" title="Redis的线程模型"></a>Redis的线程模型</h2><p>​	Redis基于Reactor模式开发了网络事件处理器，又被称为文件事件处理器。它由四个部分组成：<strong>多个套接字，IO多路复用程序，文件事件分派器，事件处理器</strong>。<u>因为文件事件分派器的数据消费是单线程的，所以Redis才叫单线程模型。</u></p>
<ul>
<li>文件事件处理器使用IO多路复用程序来同时监听多个套接字，并根据套接字目前执行的任务来为套接字关联不同的事件处理器。</li>
<li>当被监听的套接字准备好执行accpet，read等操作d时，与操作相对应的文件事件就会产生，这时文件事件处理器就会调用套接字之前关联好的事件处理器来处理这些事件。</li>
</ul>
<p>​	虽然文件事件处理器以单线程方式运行，但通过使用IO多路复用程序来监听多个套接字，文件事件处理器既实现了高性能的网络通信模型，又很好地与Redis中其他单线程方式运行的模块相对接，保持了内部设计的简单性。</p>
<h2 id="使用Redis的场景有那些"><a href="#使用Redis的场景有那些" class="headerlink" title="使用Redis的场景有那些"></a>使用Redis的场景有那些</h2><ol>
<li><strong>缓存热点数据</strong>，减缓数据库的压力</li>
<li><strong>实现计数器功能</strong>如用户点赞数、用户访问数等</li>
<li>简单的<strong>消息队列</strong></li>
<li><strong>限速器</strong>，限制某个用户访问某个接口的频率</li>
<li><strong>好友关系</strong>，利用集合的交并补实现共同好友之类的功能。</li>
</ol>
<h2 id="Redis的数据类型有那些"><a href="#Redis的数据类型有那些" class="headerlink" title="Redis的数据类型有那些"></a>Redis的数据类型有那些</h2><h3 id="String"><a href="#String" class="headerlink" title="String"></a>String</h3><p>​	是一种二进制安全的数据结构。可以是字符串、数字或者二进制。虽然Redis是由C语言写的，但并没有使用C的原生字符串，而是自己开发的<strong>SDS</strong>。<strong>不光可以保存文本数据还可以保存二进制数据</strong>，并且获取字符串长度复杂度为 O(1)，<strong>同时可以避免数据区溢出</strong>。其值最大不能超过512MB.</p>
<h3 id="Hash"><a href="#Hash" class="headerlink" title="Hash"></a>Hash</h3><p>​	是一个键值对集合，适合用于存储对象，底层由<strong>数组加链表</strong>实现</p>
<h3 id="List"><a href="#List" class="headerlink" title="List"></a>List</h3><p>​	有序可重复的集合，底层由<strong>双向链表</strong>实现。</p>
<h3 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h3><p>​	无序去重的集合，提供了交并等方法，可以用来实现共同好友等功能。</p>
<h3 id="SortedSet"><a href="#SortedSet" class="headerlink" title="SortedSet"></a>SortedSet</h3><p>​	和 Set 相比，Sorted Set 增加了一个权重参数 <code>score</code>，使得集合中的元素能够按 <code>score</code> 进行有序排列</p>
<h3 id="Bitmap"><a href="#Bitmap" class="headerlink" title="Bitmap"></a>Bitmap</h3><p>​	位图存储的是连续的二进制数字，通过 Bitmap, 只需要一个 bit 位来表示某个元素对应的值或者状态，可以大大节省存储空间。</p>
<h3 id="HyperLogLog"><a href="#HyperLogLog" class="headerlink" title="HyperLogLog"></a>HyperLogLog</h3><p>​	其特点是输入元素的数量非常大或体积非常大时，计算基数所需的空间是固定且非常小的。</p>
<h3 id="Geospatial"><a href="#Geospatial" class="headerlink" title="Geospatial"></a>Geospatial</h3><p>​	用于存储地理位置信息</p>
<h2 id="持久化机制"><a href="#持久化机制" class="headerlink" title="持久化机制"></a>持久化机制</h2><p>​	持久化就是把内存中的数据写到磁盘中，防止服务宕机导致内存数据丢失。Redis支持<strong>RDB</strong>和<strong>AOF</strong>两种方式，<strong>前者会根据指定的规则定时将内存中的数据存储在硬盘上，后者在每次执行完命令后将命令记录下来</strong>。</p>
<h3 id="RDB"><a href="#RDB" class="headerlink" title="RDB"></a>RDB</h3><p>​	Redis默认的持久化方案，会将内存中的数据写入到磁盘中，在指定目录下生成一个dump.rdb文件，重启后会加载dump文件恢复数据。Redis启动时会读取RDB快照文件，将数据从硬盘载入内存。通过RDB方式的持久化，一旦Redis异常退出就<strong>会丢失最近一次持久化以后更改的数据</strong>。</p>
<h3 id="AOF"><a href="#AOF" class="headerlink" title="AOF"></a>AOF</h3><p>​	记录下每次执行的命令，重启时重新执行AOF文件中的命令以达到恢复数据的目的。</p>
<h2 id="缓存相关的问题"><a href="#缓存相关的问题" class="headerlink" title="缓存相关的问题"></a>缓存相关的问题</h2><h3 id="缓存穿透"><a href="#缓存穿透" class="headerlink" title="缓存穿透"></a>缓存穿透</h3><p>​	查询一个<strong>不存在的数据</strong>，缓存未命中后就去查询DB，但DB里也没这个数据，所以没法更新缓存，下次查询时仍然直接作用于DB，缓存就像不存在一样。当流量大时，DB就挂掉了。</p>
<p>解决方案：</p>
<ul>
<li><strong>缓存空值</strong>，把这个不存在的key在缓存中放置一个空值，下次查询的时候就直接返回null</li>
<li><strong>布隆过滤器</strong>，如果查询的key不在布隆过滤器里就直接返回不存在。</li>
</ul>
<h3 id="缓存击穿"><a href="#缓存击穿" class="headerlink" title="缓存击穿"></a>缓存击穿</h3><p>​	大量的请求同时查询一个key时，这个key在缓存中突然过期了。然后这些请求就直接落到DB上，把DB搞挂掉了。</p>
<p>解决方案：</p>
<ul>
<li>加互斥锁，只允许一个请求查询DB，让他们排队慢慢来，防止大量的请求同时落到DB上</li>
<li>热点数据不过期：如果一个数据经常访问，可以在缓存中设置让他不过期。但要考虑数据不一致的问题。</li>
<li>缓存预热：将其存入缓存中并设置合理的过期时间。</li>
</ul>
<h3 id="缓存雪崩"><a href="#缓存雪崩" class="headerlink" title="缓存雪崩"></a>缓存雪崩</h3><p>​	设置缓存时设置的过期时间相同，导致同一时间大量缓存过期，然后DB压力过大导致挂掉</p>
<p>解决方案：</p>
<ul>
<li>在原有的失效时间上加一个随机值，降低过期时间的重复率</li>
<li>加互斥锁，防止同时大量请求落到DB上</li>
<li>二级缓存，redis失效后查二级缓存</li>
</ul>
<h2 id="Big-key（大key）"><a href="#Big-key（大key）" class="headerlink" title="Big key（大key）"></a>Big key（大key）</h2><p>​	如果一个key对应的value过大，那么这个key就是大key。除了会消耗更多的内存空间，bigkey 对性能也会有比较大的影响。</p>
<p>怎么处理：</p>
<ul>
<li>当value是String时，可以使用序列化、压缩算法将key的大小控制在合理范围内。也可以将key进行拆分，将大key拆分成几个小key</li>
<li>当value是list等集合是，根据数据规模进行分片，不同的元素计算后分到不同的片中。</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
      <tags>
        <tag>学习</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
</search>
